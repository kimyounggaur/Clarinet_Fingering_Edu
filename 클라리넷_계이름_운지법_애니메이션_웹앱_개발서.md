# 클라리넷 계이름 운지법 배우기 — 애니메이션 모바일 웹앱 개발서

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.1 (2026-07-19) — 4-렌즈 교차 검증(데이터 정합성·교수법·기술·완결성) 20건 반영 |
| 산출물 | `clarinet_solfege_trainer.html` — 단일 파일 모바일 웹앱 |
| 기반 자료 | `01 Source` 폴더 (아래 §2) |
| 문서 목표 | **이 문서 하나만 읽고** 코딩 에이전트/개발자가 앱을 구현할 수 있는 완결된 설계서 |

---

## 1. 프로젝트 개요

### 1.1 한 줄 정의

**숫자 버튼(1~7 = 도~시)을 누르면, 화면 속 클라리넷 위에서 양손 손가락이 해당 계이름의 운지로
"누르고 · 떼고 · 구멍을 막는" 모션을 실행하는 교육용 SVG 애니메이션 모바일 웹앱.**

### 1.2 학습 목표와 대상

- 대상: 클라리넷 입문~중급 학습자, 생활음악 수업 수강생, 그리고 수업 중 시연이 필요한 강사.
- 학습 목표:
  1. 계이름(고정도법) ↔ 운지의 즉각적 연결.
  2. "어느 손가락이, 어느 구멍/키를" 담당하는지 시각적으로 체득.
  3. 음이 바뀔 때 손가락이 **어떻게 움직이는지**(무엇을 떼고 무엇을 새로 누르는지) 관찰 —
     정적 운지표가 줄 수 없는 핵심 가치.

### 1.3 핵심 사용자 시나리오

1. 학생이 옥타브 `4`, 버튼 `1(도)`을 탭한다.
2. 정보 패널에 "도4 · C4 · 샬뤼모 · 실음 시♭3(B♭3)"이 표시된다.
3. 장면 속 왼손 엄지·검지·중지·약지가 순차적으로 구멍 위로 이동해 막고, 해당 키가 붉게 채워진다.
4. 버튼을 누르는 동안 실음이 재생된다(클라리넷풍 톤).
5. 이어서 `2(레)`를 탭하면 **약지 하나만 들어 올리는** 차이 모션이 재생된다 — 학생은 "도→레는
   왼손 약지만 떼면 된다"를 눈으로 이해한다.

### 1.4 완성 기준 요약 (상세는 §15)

- 42개 음 전체가 숫자 키패드로 도달 가능하고, 운지 데이터가 원본과 diff 0.
- 모든 음 전환에서 손가락 누름/떼기/이동 애니메이션이 60fps로 재생.
- iPhone Safari · Android Chrome에서 `file://` 더블클릭 실행 포함 정상 동작.

---

## 2. 소스 자료와 기준 선언

경로: `01 Source/` (모든 파일은 **읽기 전용**으로 취급한다)

| 파일 | 역할 |
|---|---|
| `clarinet_standard_fingering_chart.html` | **운지 데이터의 단일 기준(source of truth)**. `COMPONENTS`(25개 키 좌표), `ROUND_IDS`, `HORIZONTAL_IDS`, `DATA`(42음·기본 42/대체 19), `REGISTER` 상수를 여기서 코드로 추출한다. |
| `clarinet_fingering_chart_upgrade_vibe_prompt(GPT56).md` | 손·손가락 모델의 기준 문서. 손가락 ID 체계, 키→손가락 접촉 규칙, 연동 키 예외, 손 SVG 설계 원칙을 승계한다. |
| `clarinet_solfege_hand_fingering_chart(GPT56).html` | 손 SVG가 실제로 구현된 참조 구현. `activeFingers`, `fingerTarget`, `fingerMarkup`, `handsMarkup`, `diagramSVG`, `SOLFEGE` 맵, 이조(실음) 계산, URL 해시, 검색 정규화 로직을 재사용 기반으로 삼는다. |
| `clarinet_standard_fingering_chart.svg/.pdf`, `clarinet_solfege_hand_fingering_chart(GPT56).svg/.pdf` | 시각 결과물 레퍼런스(인쇄용). 본 앱에서는 직접 사용하지 않는다. |
| `클라리넷_표준_운지표(Fable5).html/.svg/.pdf` | 동일 데이터 기반의 포스터형 변형본. 시각 스타일 참고용. |

> **원칙**: 운지 데이터·키 좌표는 눈으로 재입력하지 않고 원본 HTML에서 **프로그램으로 추출**하여
> 그대로 붙여넣는다(§14.1의 회귀 테스트로 검증).

---

## 3. 불변 규칙 (마스터 프롬프트 §2 승계 + 웹앱 추가 규칙)

1. 42개 음의 순서와 MIDI 52–93 연속성을 바꾸지 않는다.
2. 원본 `primary` / `alternate` 배열에서 키 ID를 추가·삭제·교체하지 않는다.
3. 대체 운지가 없는 음에 임의 운지를 추가하지 않는다.
4. `COMPONENTS` 25개 좌표, `ROUND_IDS`, `HORIZONTAL_IDS`를 변경하지 않는다.
5. 손·손가락·악기는 전부 **독자 제작 벡터**. 래스터 이미지·사진 트레이싱 금지.
6. **외부 CDN·폰트·라이브러리·이미지 요청 0건**. 네트워크 요청 0건.
7. HTML은 `file://` 더블클릭으로 동작하는 **완전한 단일 파일**.
8. 오른손 엄지(RT)는 절대 "누름 손가락"으로 집계하지 않는다 — 항상 엄지받침 지지.
9. (웹앱 추가) 애니메이션을 위해 장면 DOM은 **1회 생성 후 속성만 갱신**한다.
   원본 참조 구현처럼 `innerHTML` 재생성으로 갱신하지 않는다 — 재생성은 전환 모션을 불가능하게 한다.
10. (웹앱 추가) `prefers-reduced-motion: reduce`에서는 모든 모션을 즉시 상태 변경으로 대체한다.

---

## 4. 도메인 모델

### 4.1 음 데이터 (42음)

원본 `DATA` 배열의 각 원소:

```js
{
  index: 1,              // 1..42
  midi: 52,              // 기보음 MIDI. 52(E3)..93(A6) 연속
  id: "E3",              // URL·검색용 ASCII id (♯ → s. 예: "Fs4")
  primaryName: "E3",     // 영문 음이름 (♯/♭ 유니코드)
  enharmonic: "",        // 이명동음 ("G♭3" 등, 없으면 "")
  display: "E3",
  register: "chalumeau", // chalumeau | throat | clarion | altissimo
  registerKo: "샬뤼모",
  primary: [3,4,8,...],  // 기본 운지: 닫는 키 ID 집합
  alternate: [...]|null  // 대체 운지 (19개 음만 보유)
}
```

음역 구분 (원본과 동일):

| register | 한글 | 기보 범위 | MIDI | 음 개수 | 색(배지/카드 띠) |
|---|---|---|---|---|---|
| chalumeau | 샬뤼모 | E3–F♯4 | 52–66 | 15 | `#E8F3EE` / `#3C8062` |
| throat | 목음역 | G4–B♭4 | 67–70 | 4 | `#FFF3D6` / `#B48025` |
| clarion | 클라리온 | B4–C6 | 71–84 | 14 | `#E8EEF9` / `#4A71AA` |
| altissimo | 알티시모 | C♯6–A6 | 85–93 | 9 | `#F5E8F4` / `#99518E` |

### 4.2 키/구멍 컴포넌트 (25개)

- `COMPONENTS[i] = [x, y, w, h]` (i = 0..24). 좌표계는 원본 추상 키맵
  (대략 x −12..10, y −12..68, 위 = 마우스피스 쪽).
- `ROUND_IDS = {3,4,5,8,11,13,17,18,20}` → `<ellipse>`로 렌더.
- `HORIZONTAL_IDS = {2,7,12,14,19}` → 가로형 `<rect>`(rx 1.45), 나머지는 세로형 `<rect>`(rx 1.15).
- 각 키의 의미와 담당 손가락은 **부록 B** 표 참조.

### 4.3 손가락 모델 (10개)

```text
LT = 왼손 엄지      L1 = 왼손 검지   L2 = 왼손 중지   L3 = 왼손 약지   L4 = 왼손 새끼
RT = 오른손 엄지(엄지받침 지지 전용 — 운지 집계 제외)
R1 = 오른손 검지   R2 = 오른손 중지   R3 = 오른손 약지   R4 = 오른손 새끼
```

#### 4.3.1 키 → 손가락 직접 접촉 표

| 손가락 | 담당 키 ID | 비고 |
|---|---|---|
| LT | 1, 3 | 레지스터 키(1) 또는 엄지 구멍(3). 둘 다 활성이면 엄지가 **동시에** 담당 |
| L1 | 0, **2**, 4 | A 키(0), 목음역 G♯/A♭ 키(2), 왼손 검지 구멍(4) |
| L2 | 8 | 왼손 중지 구멍 |
| L3 | 13 | 왼손 약지 구멍 |
| L4 | 9, 10, 11, 12, 14, 15, 16 | 왼손 새끼 레버 군 |
| R1 | 5, 6, 7, 17 | 사이드 키(5,6,7), 오른손 검지 구멍(17) |
| R2 | 18, **19** | 오른손 중지 구멍(18), 슬리버 키(19 — 재배정, 아래 참조) |
| R3 | 20 | 오른손 약지 구멍 |
| R4 | 21, 22, 23, (24) | 오른손 새끼 레버 군. 24는 아래 연동 예외 적용 |
| RT | 없음 | 항상 엄지받침 지지. 누름 집계 금지 |

> #### ⚠️ 마스터 프롬프트 대비 수정 사항 2건 (손가락 교육 레이어 교정)
>
> 불변 규칙(§3)이 보호하는 운지 데이터 자체는 변경되지 않는다. 아래는 키→손가락 접촉
> 모델의 교정이다. (감수: 실연주 기준 확인 완료 — 필요시 강사가 재검증할 것.)
>
> **1) 키 2 (목음역 G♯/A♭ 키): L4 → L1**
> 마스터 프롬프트 §5.2와 GPT56 구현은 키 2를 왼손 새끼(L4)에 배정했다. 그러나 실제 주법에서
> 스로트 G♯/A♭ 키는 A 키 바로 위에 있어 **왼손 검지(L1)**로 누른다. 키 2는 키맵 최상단
> (y ≈ −9..2)에 있어 L4가 도달하는 모션은 시각적으로도 비자연스럽다.
> 영향 음: 솔♯4/라♭4 (`primary: [2]`) 1개.
>
> **2) 키 19 (슬리버 키): R1 → R2**
> 키 19의 사용례 4건(시3 대체·파♯5 대체·레♯6·솔♯6)은 전부 키 17(R1 구멍)이 함께 활성이다.
> R1 소속을 유지하면 우선순위상 R1의 목표가 항상 17이 되어, 키 19가 앱 전체에서 **한 번도
> "직접 누름"으로 표시되지 못하고** 연동 키(점선)로 잘못 렌더링된다. 4건 모두 키 18(R2 구멍)이
> 열려 있고 키 19는 R2 구멍 바로 아래에 있으므로 **오른손 중지(R2)**가 담당하는 것이
> 해부학적으로도 자연스럽다. (GPT56 `fingerTarget`의 R3:[20,19] 우선순위는 19와 20이 공존하는
> 패턴이 없어 실행 불가능한 데드 코드였다.)

#### 4.3.2 연동 키 예외 (R4 집계 규칙)

원본 패턴에서 왼손 새끼 레버 15 또는 16이 활성일 때 하단 패드 24가 함께 활성화되는 경우가
있다(E/B·F♯/C♯ 좌우 연동). 이때 24는 기계적으로 닫히는 것이므로 **오른손 새끼 접촉으로
집계하지 않는다**.

```js
const rightPinkyPressed =
  [21, 22, 23].some(id => pattern.includes(id)) ||
  (pattern.includes(24) && !pattern.includes(15) && !pattern.includes(16));
```

#### 4.3.3 activeFingers (확정 코드)

```js
const FINGER_ORDER = ["LT","L1","L2","L3","L4","R1","R2","R3","R4"];

function activeFingers(pattern) {
  const p = new Set(pattern), out = new Set();
  if ([1, 3].some(id => p.has(id))) out.add("LT");
  if ([0, 2, 4].some(id => p.has(id))) out.add("L1");     // 키 2 → L1 (재배정)
  if (p.has(8))  out.add("L2");
  if (p.has(13)) out.add("L3");
  if ([9,10,11,12,14,15,16].some(id => p.has(id))) out.add("L4");
  if ([5,6,7,17].some(id => p.has(id))) out.add("R1");
  if ([18,19].some(id => p.has(id))) out.add("R2");   // 키 19 → R2 (재배정)
  if (p.has(20)) out.add("R3");
  if ([21,22,23].some(id => p.has(id)) ||
      (p.has(24) && !p.has(15) && !p.has(16))) out.add("R4");
  return FINGER_ORDER.filter(f => out.has(f));
}
```

#### 4.3.4 손가락 목표점 fingerTarget

각 손가락은 "대기 목표점(비활성)"과 "키 목표점(활성)"을 가진다. GPT56 구현을 승계하되
키 2 재배정과 R3 우선순위를 정리한다.

```js
const PRIORITY = {                       // 활성 시 목표 키 선택 우선순위
  LT: [1, 3],
  L1: [4, 0, 2],
  L2: [8], L3: [13],
  L4: [10, 9, 11, 12, 14, 15, 16],
  R1: [17, 5, 7, 6],
  R2: [18, 19], R3: [20],
  R4: [21, 22, 23, 24],
};
const REST = {                           // 비활성(대기) 목표점 [x, y]
  LT: [-12.7, 1.5], L1: [7.2, 2.7],  L2: [7.6, 12.7], L3: [7.6, 18.9], L4: [8.5, 25],
  R1: [-7.7, 35.2], R2: [-7.7, 41.6], R3: [-7.7, 50.1], R4: [-8.4, 58.4],
};

function fingerTarget(finger, pattern, active) {
  if (!active) return { key: null, xy: REST[finger] };
  const p = new Set(pattern);
  let chosen = null;
  for (const id of PRIORITY[finger]) {
    if (!p.has(id)) continue;
    if (finger === "R4" && id === 24 && (p.has(15) || p.has(16))) continue; // 연동 예외
    chosen = id; break;
  }
  if (chosen === null) return { key: null, xy: REST[finger] };
  let [cx, cy] = center(chosen);                       // 키 중심
  if (["L1","L2","L3"].includes(finger) && [4,8,13].includes(chosen)) cx = 2.8;
  else if (["R1","R2","R3"].includes(finger) && [17,18,20].includes(chosen)) cx = -3.1;
  else if (finger === "LT") cx -= 0.5;
  else if (finger.startsWith("L")) cx += 1.1;
  else cx -= 1.1;
  return { key: chosen, xy: [cx, cy] };
}
```

#### 4.3.5 직접 접촉 키 vs 연동 키 (시각 구분)

마스터 프롬프트의 "직접 접촉 손가락과 기계적으로 연동되어 닫히는 키의 구분" 요구를 다음
규칙으로 일반화한다.

```js
function keyRoles(pattern) {
  const states = fingerStateMap(pattern);         // finger → {active, key, xy}
  const direct = new Set(
    Object.values(states).filter(s => s.active && s.key != null).map(s => s.key));
  // 한 손가락이 인접 키를 동시에 담당하는 예외 3종
  if (states.LT.active) [1, 3].forEach(k => { if (pattern.includes(k)) direct.add(k); });   // 엄지: 레지스터 키+엄지 구멍
  if (states.R1.active) [5, 7].forEach(k => { if (pattern.includes(k)) direct.add(k); });   // 사이드 키 쌍 5+7
  if (states.L4.active) [9, 10].forEach(k => { if (pattern.includes(k)) direct.add(k); });  // 왼손 레버 쌍 9+10
  return new Map(pattern.map(k => [k, direct.has(k) ? "direct" : "linked"]));
}
```

- **direct 키**: 붉은 채움 `#C12B43` + 실선 외곽 `#7F1D2D`. 손가락이 직접 누르는 키와,
  같은 손가락이 인접 키를 함께 누르는 경우(LT의 1·3, R1의 5+7, L4의 9+10)를 포함한다.
- **linked 키** = **기계 연동으로 닫히는 키**. 이 데이터셋에서는 **11→12**(C♯/G♯ 레버→패드)와
  **15/16→24**(좌우 새끼손가락 연동) 두 경우뿐이다. 동일 붉은 채움 + **점선 외곽**
  (`stroke-dasharray: 1.6 1.1`). 범례에 "연동으로 닫히는 키"로 표기.
- 위 예외 처리 후 linked로 판정되는 키가 12·24 외에 존재하면 접촉 모델 오류다
  (§14.2 어설션으로 강제).

### 4.4 계이름·이조 모델

#### 4.4.1 고정도법 계이름 (원본 SOLFEGE 승계)

```js
const SOLFEGE_KO = {
  "C":"도","C♯":"도♯","D♭":"레♭","D":"레","D♯":"레♯","E♭":"미♭","E":"미",
  "F":"파","F♯":"파♯","G♭":"솔♭","G":"솔","G♯":"솔♯","A♭":"라♭",
  "A":"라","A♯":"라♯","B♭":"시♭","B":"시"
};
```

#### 4.4.2 기보음과 실음

- B♭ 클라리넷 실음 MIDI = 기보 MIDI − 2 (장2도 아래)
- A 클라리넷 실음 MIDI = 기보 MIDI − 3 (단3도 아래)
- 운지·키패드·표시는 **항상 기보음 기준**. 악기 선택은 실음 표시와 소리 재생 주파수만 바꾼다.
- **실음(이조 결과) 표기는 플랫 계열 우선**: `midiName(midi, prefer = "flat")`처럼 표기 선호
  인자를 두고, 실음 표시 경로(`soundingFor`)는 플랫 배열
  `["C","D♭","D","E♭","E","F","G♭","G","A♭","A","B♭","B"]`을 사용한다.
  예: 기보 도4(MIDI 60) → B♭ 실음 58 → **시♭3(B♭3)** (라♯3 아님).
  GPT56 참조 구현의 `midiName`은 샤프 전용 배열이므로 실음 표기에 그대로 재사용하지 않는다.

#### 4.4.3 표기 모드 (설정)

`계이름 + 영문`(기본) / `계이름만` / `영문 음이름만` — GPT56과 동일한 3모드.

---

## 5. 숫자 키패드 설계 (핵심 인터랙션)

### 5.1 매핑 원칙

숫자보(1=도 … 7=시) 관례를 따른다. **버튼 조합 = 옥타브 × 숫자 × 변화표**.

```text
숫자 버튼:  1=도  2=레  3=미  4=파  5=솔  6=라  7=시   (고정도법·기보음 기준)
옥타브:     [3] [4] [5] [6]        (세그먼트 컨트롤, 기본 4)
변화표:     [♭] [♮] [♯]           (3상태 토글, 기본 ♮)
```

### 5.2 버튼 → MIDI → 음 (확정 코드)

```js
const PC  = { 1:0, 2:2, 3:4, 4:5, 5:7, 6:9, 7:11 };   // 도레미파솔라시 반음 오프셋
const ACC = { flat:-1, natural:0, sharp:+1 };

function midiFromButton(octave, button, acc) {
  return (octave + 1) * 12 + PC[button] + ACC[acc];    // C4 = 60 기준
}
// 예: (4, 1, natural) → 60 (도4)   (3, 3, natural) → 52 (미3 = 최저음)
//     (6, 6, natural) → 93 (라6 = 최고음)

const NOTE_BY_MIDI = new Map(DATA.map(n => [n.midi, n]));
```

### 5.3 비활성(disabled) 규칙

버튼 상태는 현재 옥타브·변화표 선택에 따라 매 갱신 시 계산한다.

1. **음역 밖**: `midi < 52 || midi > 93` → 비활성.
   - 옥타브 3: 버튼 1(도3), 2(레3) 항상 비활성. 미♭3(51)도 비활성.
   - 옥타브 6: 버튼 7(시6=95) 항상 비활성. 라♯6/시♭6(94)도 비활성.
   - 탭 시 토스트: `"이 음은 클라리넷 음역 밖이에요 (미3~라6)"`.
2. **이명동음 없는 변화표**: ♯는 미·시에서, ♭는 도·파에서 비활성 (미♯4=파4, 도♭4=시3 등
   원본 DATA에 존재하지 않는 표기). 탭 시 토스트 문안은 하드코딩하지 말고
   `midiFromButton`으로 계산한 MIDI의 실제 표기(**옥타브 포함**)로 생성한다
   (자동 이동은 하지 않음 — 학습 혼동 방지). 도♭·시♯은 옥타브 경계를 넘는 점에 주의:
   - 미♯O → `"미♯O는 파O와 같은 음이에요"` · 파♭O → `"파♭O는 미O와 같은 음이에요"`
   - 도♭O → `"도♭O는 시(O−1)와 같은 음이에요"` (예: 도♭4 = 시3)
   - 시♯O → `"시♯O는 도(O+1)와 같은 음이에요"` (예: 시♯4 = 도5)
3. 비활성 시각: 불투명도 0.35 + `aria-disabled="true"`. 탭 시 짧은 진동 없는 셰이크 모션 허용.
4. **토스트 공통 명세**: 화면 하단 중앙, 2초 후 자동 소멸, 표시 중 재탭 시 타이머 리셋
   (중복 생성 금지), 컨테이너는 `role="status"`(스크린리더에 polite 고지).

### 5.4 이명동음 표시 규칙

사용자가 ♭로 입력한 경우(예: 레♭4) 정보 패널 제목은 **입력한 표기 우선**으로 표시하고
부제에 이명동음을 보인다: `레♭4 (D♭4) · = 도♯4`. 내부적으로는 동일한 note 객체(MIDI 61)다.

```js
function displayFor(note, inputAcc) {
  const useEnh = inputAcc === "flat" && note.enharmonic;   // DATA는 ♯ 표기가 primaryName
  return {
    title: useEnh ? note.enharmonic : note.primaryName,
    sub:   useEnh ? note.primaryName : note.enharmonic,    // "" 가능
  };
}
```

### 5.5 도달성 보증

부록 A의 42행 매핑 표대로, 42개 음 전부가 최소 1개 버튼 조합으로 도달 가능해야 하며
부팅 시 어설션으로 검증한다 (§14.2).

---

## 6. 화면 설계 (모바일 우선)

### 6.1 세로 레이아웃 (기준 뷰포트 360×740)

```text
┌─────────────────────────────────┐
│ ♪ 클라리넷 운지 트레이너      ⚙ │  ← 헤더 44px (설정 시트 열기)
├─────────────────────────────────┤
│ [샬뤼모]  도4 · C4              │  ← 정보 패널 88px
│ 실음 시♭3(B♭3) · 기본 운지      │     (aria-live="polite")
│ 🖐 LT L1 L2 L3   [기본|대체]    │  ← 손가락 칩(가로 스크롤) + 대체 스위치
├─────────────────────────────────┤
│                                 │
│        ┌───┐                    │
│   왼손 │ ● │ ← 악기+양손 SVG    │  ← 장면 영역 (flex: 1)
│  ~~~~~ │ ● │ ~~~~~ 오른손       │     viewBox "-34 -21 68 105"
│        │ ○ │                    │     세로형이라 폰 화면과 궁합 좋음
│        └───┘                    │
├─────────────────────────────────┤
│  옥타브  [3] [4]̲ [5] [6]        │  ← 키패드 영역 ~230px
│  변화표  [♭] [♮]̲ [♯]   [♪on]   │     (터치 타깃 최소 44×44px, 권장 48px)
│ ┌───┬───┬───┬───┬───┬───┬───┐  │
│ │ 1 │ 2 │ 3 │ 4 │ 5 │ 6 │ 7 │  │  ← 숫자 버튼 1행 7개
│ │도 │레 │미 │파 │솔 │라 │시 │  │     높이 64px (좁은 폭 보상)
│ └───┴───┴───┴───┴───┴───┴───┘  │
└─────────────────────────────────┘
```

- 숫자 버튼: 360px 폭에서 7개 × 46px + 간격 4px = 346px로 1행 배치 가능.
  **1행 유지가 원칙**(음높이 순서의 멘탈 모델 보존). 344px 미만에서는 간격을 2px로 줄여
  버튼 폭 44px을 유지한다(7×44+6×2=320px — 최소 뷰포트 320px까지 44px 기준 충족).
- 버튼 라벨: 숫자(크게) + 계이름(작게) 2단. 변화표가 ♯/♭이면 계이름 라벨에 즉시 반영
  (예: ♯ 선택 시 `1` 아래 `도♯`).
- 키패드는 엄지 존(하단)에 고정. 장면 영역이 남는 높이를 모두 차지.
- `height: 100vh`를 먼저 선언하고 `100dvh`로 덮어쓴다(iOS Safari 15.0–15.3은 dvh 미지원 —
  캐스케이드 폴백). `env(safe-area-inset-bottom)` 패딩. `touch-action: manipulation`,
  `-webkit-tap-highlight-color: transparent`.

### 6.2 가로 레이아웃 (`@media (orientation: landscape) and (max-height: 500px)`)

장면을 왼쪽(높이 100%), 정보 패널+키패드를 오른쪽 컬럼으로. 그 외 규칙 동일.

### 6.3 설정 시트 (⚙ → 하단 슬라이드 시트)

| 설정 | 값 | 기본 |
|---|---|---|
| 악기 | B♭ 클라리넷 / A 클라리넷 | B♭ |
| 표기 | 계이름+영문 / 계이름 / 영문 | 계이름+영문 |
| 소리 | 켬/끔, 실음 재생/기보음 재생 | 켬 · 실음 |
| 손 가이드 | 켬/끔 (끄면 악기+키만) | 켬 |
| 손가락 라벨 | 켬/끔 (LT·L1… 콜아웃) | 켬 |
| 애니메이션 속도 | 보통(1×) / 천천히(0.5×) / 즉시 | 보통 |

### 6.4 디자인 토큰 (소스 승계)

```css
:root {
  --ink:#17212b; --muted:#5f6f7b; --line:#d5dee4; --paper:#f2f5f7; --card:#fff;
  --pressed:#b51f32; --pressed-key:#c12b43; --pressed-dark:#7f1d2d;
  --skin:#f2c6a7; --skin-line:#a66c51;
  /* 음역: §4.1 표의 배지/띠 색 */
}
font-family: NanumGothic, "Noto Sans CJK KR", "Apple SD Gothic Neo", system-ui, sans-serif;
```

### 6.5 물리 키보드 지원 (Phase 5 필수)

숫자 `1–7` = 계이름 버튼, `↑/↓` = 옥타브, `+/-` = 변화표. 해당 컨트롤에
`aria-keyshortcuts`를 명시한다(§11). `◀▶` 반음 이동 버튼·`←/→` 키·스와이프 제스처는
MVP 범위가 아니며 §16 v2 확장("반음 탐색 강화")으로 이관한다.

---

## 7. SVG 장면 설계

### 7.1 캔버스와 레이어

```html
<svg id="scene" viewBox="-34 -21 68 105" role="img" aria-label="...동적 갱신...">
  <defs><!-- bodyWood·skinGrad 그라디언트 (GPT56 defsMarkup 승계) --></defs>
  <g id="palms"><!-- 손바닥 2개+수근부, opacity .83, 악기 뒤 --></g>
  <g id="clarinet"><!-- 마우스피스·배럴·상관·하관·벨·연결링·키로드 + 25개 키(열림 상태) --></g>
  <g id="fingers"><!-- 손가락 9개 <g data-finger> + RT 지지 --></g>
  <g id="pressedKeys"><!-- 활성 키 오버레이 (동적) --></g>
  <g id="fingertips"><!-- 손끝 ellipse + 접촉점 circle (활성 키 위) --></g>
  <g id="labels"><!-- LT/L1… 점선 콜아웃 (토글) --></g>
</svg>
```

- 레이어 순서(아래→위): 손바닥 → 악기·열린 키 → 손가락 → 활성 키 오버레이 → 손끝 팁 → 라벨.
  (마스터 프롬프트 §6.2·§7.3의 순서를 따른다. **주의**: GPT56 `diagramSVG`는
  `${hands}${clarinetBase()}${active}` 순서로 조립해 손가락·라벨이 악기 **뒤**에 깔리는
  편차가 있으므로, 그 조립 순서를 그대로 이식하지 말 것.)
- **가림 처리**: 손끝이 키 도형 안에 들어가는 손가락(LT의 키 1·3, L4·R4 레버류, R2의 키 19)은
  불투명한 활성 키가 접촉점·스쿼시 임팩트를 가린다. 이를 막기 위해 각 손가락의 손끝
  ellipse + 접촉점 circle은 활성 키 오버레이 **위**의 전용 레이어 `#fingertips`에 둔다
  (손가락 몸통 path·halo·관절은 `#fingers`에 유지).
- 악기 본체·손바닥·RT 지지는 **부팅 시 1회 생성 후 불변**.
- 손가락 9개는 **영속 노드**: `#fingers`의 `<g data-finger="L1">`에 halo path·외곽 path·
  피부 path·관절 circle을, `#fingertips`의 `<g data-fingertip="L1">`에 손끝 ellipse·접촉점
  circle을 둔다. 매 프레임 `d`·`cx`·`cy` 속성만 갱신하며 `setFingerTip`이 두 그룹을 함께 갱신한다.
- 활성 키 오버레이: 25개 키 노드를 미리 만들어 두고 `class`(open/direct/linked)만 토글한다.

### 7.2 손가락 벡터 (GPT56 `fingerMarkup` 승계)

시작점(손바닥 쪽 고정):

```js
const STARTS = { LT:[-15.9,3.1], L1:[16.4,3], L2:[18.2,10.1], L3:[18.4,17.4], L4:[16.7,23.9],
                 R1:[-16.3,35.4], R2:[-18.2,42], R3:[-18,49.4], R4:[-16.2,55.7] };
```

경로: `M bx,by C c1x,c1y c2x,c2y tx,ty` — 제어점은 끝점의 함수:

```js
if (finger === "LT")            { c1=[-14, 1.2];                       c2=[tx-1.5, ty]; }
else if (finger.startsWith("L")){ c1=[13,  by+(ty-by)*.18];            c2=[8.2,  ty-.8]; }
else                            { c1=[-13, by+(ty-by)*.18];            c2=[-8.4, ty-.8]; }
```

→ **끝점 (tx,ty)만 보간하면 전체 경로가 자연스럽게 따라온다** (§8 애니메이션의 근거).

스타일 (활성/비활성):

| 요소 | 비활성 | 활성 |
|---|---|---|
| 외곽 stroke | `#A66C51` w5.5 | `#B52D45` w5.5 |
| 피부 stroke | `#F2CFB4` w4.3 | `#F3BE98` w4.3 |
| 손끝 ellipse | rx2.2 ry2.55 피부색 | 동일 (전체를 빨갛게 칠하지 않음) |
| 접촉점 circle | r1.05 흰 채움+갈색 선 | r1.05 `#B51F32` 채움+`#7F1D2D` 선 |
| halo | 없음 | 경로 위 `#B51F32` w6.7 opacity .26 |

- 비활성 손끝은 키에서 4–7 SVG 단위 떨어진 대기점(`REST`)에 위치한다.
- RT: 하관 오른쪽 `M8,43.5 C12,43 14.4,46.8 13.1,50.4` + 엄지받침 rect. 항상 표시, 항상 중립색.

### 7.3 손가락 라벨 콜아웃 (GPT56 승계)

`[fid, lx, ly, ex, ey]` 명세로 점선 리더 + 텍스트. 활성 `#9F2338` / 비활성 `#65727C`.
장면 크기가 작아지는 세로 모드에서 겹침 방지를 위해 폰트 3.4 유지, 라벨 토글 기본 켬.

---

## 8. 애니메이션 설계 (본 앱의 핵심)

### 8.1 전환 모델 — 손가락 단위 diff

키 집합이 아니라 **손가락 상태의 diff**로 계산한다. 손가락이 계속 활성이어도 담당 키가
바뀌는 경우(레버 이동)가 있기 때문이다.

```js
// fingerStateMap(pattern): FINGER_ORDER 9개 각각에 대해
//   { active: bool, key: number|null, xy: [x,y] }  (fingerTarget 이용)

function diffTransition(prevPattern, nextPattern, currentXY = null) {
  // currentXY: 인터럽트 시 손가락별 동결 좌표 맵 (§8.4). 평시에는 null.
  const prev = fingerStateMap(prevPattern), next = fingerStateMap(nextPattern);
  const moves = [];
  for (const f of FINGER_ORDER) {
    const a = prev[f], b = next[f];
    let type = "HOLD";
    if (!a.active &&  b.active) type = "PRESS";
    else if ( a.active && !b.active) type = "RELEASE";
    else if ( a.active &&  b.active && a.key !== b.key) type = "SLIDE";
    else if (!a.active && !b.active) type = "IDLE";
    moves.push({ finger: f, type, from: currentXY?.[f] ?? a.xy, to: b.xy });
  }
  // SLIDE 손가락이 떠나는 이전 키는 B 트랙이 단독 소유한다 (§8.2)
  const slidePrev = moves.filter(m => m.type === "SLIDE").map(m => prev[m.finger].key);
  return {
    moves,
    keysOff: prevPattern.filter(k => !nextPattern.includes(k) && !slidePrev.includes(k)),
    keysOffSlide: slidePrev.filter(k => !nextPattern.includes(k)),   // B 트랙에서 해제
    keysOn:  nextPattern.filter(k => !prevPattern.includes(k)),
    roles:   keyRoles(nextPattern),          // direct | linked
  };
}
```

### 8.2 타임라인 사양 (기본 속도 1×)

| # | 트랙 | 대상 | 시작 시점 | 지속 | 이징 | 내용 |
|---|---|---|---|---|---|---|
| A1 | 키 해제 | `keysOff` | 담당 RELEASE 손가락의 A2 시작 시점(40ms×i) · 담당 손가락 없는 키(연동 해제·HOLD 이탈 키)는 0ms | 100ms | ease-out | 붉은 채움 → 열림(흰색). SLIDE 이전 키는 제외(`keysOffSlide` — B 트랙 단독 소유) |
| A2 | 손가락 떼기 | RELEASE | 0ms + 40ms×i | 140ms | `cubic-bezier(.4,0,.6,1)` | 목표점 → 대기점. **아래→위** 순(i: y 내림차순) |
| B | 슬라이드 | SLIDE | 80ms | 280ms (60+140+80) | 구간별 | 들기(60ms, 대기점 방향 2단위) → 이동(140ms) → 누름(80ms). 이전 키(`keysOffSlide`)는 들기 완료 시 해제, 새 키는 접촉 시 채움 |
| C1 | 손가락 누름 | PRESS | 120ms + 55ms×i | 180ms | `cubic-bezier(.2,.8,.2,1)` | 대기점 → 목표점. **위→아래** 순(i: y 오름차순) |
| C2 | 키 채움 | 각 direct 키 | 담당 손가락 progress 0.85 시점 | 120ms | ease-out | 열림 → `#C12B43` |
| C3 | 연동 키 채움 | linked 키 | C1 첫 손가락 접촉 시점 | 150ms | ease-out | 점선 외곽으로 채움 |
| C4 | 누름 임팩트 | 접촉 순간 | 접촉 시 | 80ms | ease-out | 손끝 ellipse 스쿼시: ry×0.85, rx×1.1 후 복원 |
| C5 | halo | 접촉 후 | 접촉 시 | 150ms | linear | opacity 0 → .26 |
| — | HOLD 손가락 | HOLD | — | — | — | 모션 없음. 새로 패턴에 든 담당 키(예: 시4의 키 3)는 **전환 시작 0ms**에 C2의 지속·이징(120ms ease-out)으로 채움 — HOLD는 트윈이 없어 progress 앵커가 없으므로 0ms 고정 |

- 총 소요: 통상 400–700ms (9손가락 최악의 경우 ≈ 120+55×8+180 ≈ 740ms).
- `천천히(0.5×)` 설정: 모든 지속·지연 2배. `즉시`: 전 트랙 0ms.
- `prefers-reduced-motion: reduce` → 강제 `즉시` + 설정 UI에서 잠금 표시.

### 8.3 구현 — rAF 보간 엔진

CSS 키프레임이 아니라 **requestAnimationFrame 루프에서 손끝 좌표를 보간**하고
경로 `d`를 매 프레임 재생성한다(§7.2의 제어점 공식 덕분에 끝점 보간만으로 충분).

```js
// 트윈: { finger, from:[x,y], to:[x,y], t0, dur, ease, onContact }
function tick(now) {
  let alive = false;
  for (const tw of tweens) {
    const t = tw.dur <= 0 ? 1 : clamp((now - tw.t0) / tw.dur, 0, 1);   // dur=0 가드
    if (t < 1) alive = true;
    const e = tw.ease(t);
    const x = lerp(tw.from[0], tw.to[0], e), y = lerp(tw.from[1], tw.to[1], e);
    scene.setFingerTip(tw.finger, x, y);          // path d + ellipse + 접촉점 갱신
    if (!tw.contacted && t >= 0.85 && tw.onContact) { tw.contacted = true; tw.onContact(); }
  }
  if (alive) raf = requestAnimationFrame(tick);
}
```

- 성능: 9개 path × 60fps 속성 갱신은 모바일에서 충분히 가볍다(레이아웃 유발 없음, SVG 페인트만).
  장면 밖 요소 재생성 금지. `will-change` 불필요.
- 키 채움(A1·C2·C3)은 CSS `transition: fill .12s, stroke .12s`로 처리하고 JS는 class만 토글.
- **즉시 모드**(속도 `즉시` 또는 `prefers-reduced-motion`): 트윈·rAF 루프를 아예 만들지 않는다.
  `setFingerTip`을 각 손가락의 최종 좌표로 1회 호출하고, 키 채움 상태를 새 패턴 기준으로
  동기 재판정하며, 임팩트·halo 등 접촉 연출은 생략한다.

### 8.4 인터럽트(연타) 정책

애니메이션 중 새 입력이 오면:

1. 진행 중 트윈을 **현재 보간 위치에서 동결**하고 타임라인을 폐기한다(점프 금지).
2. `diffTransition(직전 전환의 목표 패턴, 새 패턴, currentXY)`를 호출해 즉시 재생한다.
   `currentXY`는 scene에 마지막으로 적용된 손끝 좌표 맵(동결 위치)이며 각 트윈의 시작점을
   치환한다(§8.1 시그니처). type 분류·키 diff는 직전 전환의 **목표 패턴** 기준으로 계산한다.
3. 키 채움 상태는 새 패턴 기준으로 즉시 재판정한다.

→ 빠른 연타(예: 도–레–미 스케일 연습)에서도 손가락이 끊기지 않고 흐르듯 따라온다.

### 8.5 워크드 예제 (구현 검증용 필수 시나리오)

**예제 1 — 도4 → 레4** (`[3,4,8,13]` → `[3,4,8]`)
- RELEASE: L3 (키 13 해제). HOLD: LT·L1·L2. 다른 트랙 없음.
- 기대: 왼손 약지 하나만 들어 올려지고 키 13이 하얗게 열린다.

**예제 2 — 시♭4 → 시4 "브레이크 통과"** (`[0,1]` → `[1,3,4,8,13,15,17,18,20,24]`)
- LT: 두 패턴 모두 목표 키 1 → HOLD. 단, 키 3이 새로 패턴에 들어오므로 전환 시작 0ms에
  C2 규칙(120ms ease-out)으로 채움(엄지가 1·3 동시 담당 — `keyRoles`의 LT 예외로 direct 판정).
- L1: 활성 유지, 목표 0 → 4 → **SLIDE**. 키 0은 A1이 아니라 B 트랙 소유(`keysOffSlide`) —
  들기 완료(≈140ms)에 해제되고, 키 4는 접촉 시 채워진다. (검증 포인트)
- PRESS: L2(8), L3(13), L4(15), R1(17), R2(18), R3(20) — 6손가락 스태거 순차 누름.
- R4: 24는 15와 동시 활성 → **집계 안 됨**. 키 24는 **linked**(점선)로 채워짐.
- 기대: 목음역→클라리온 도약에서 오른손이 통째로 내려와 잡는 모션이 재생된다.

**예제 3 — 아무 음 → 솔4 (전부 열림)** (`primary: []`)
- 전 손가락 RELEASE, 전 키 해제. 손가락 칩 영역에 "모든 구멍 열림" 표시. RT 지지는 유지.

**예제 4 — 미3 (최다 손가락)** (`[3,4,8,13,15,17,18,20,24]`)
- 활성: LT L1 L2 L3 L4(15) R1(17) R2 R3 = 8손가락. 24는 linked.
- 기대: 스태거가 위→아래로 물 흐르듯 재생(총 ≈ 700ms), 60fps 유지.

**예제 5 — 솔♯4/라♭4 (키 2 재배정 확인)** (`primary: [2]`)
- L1이 위로 뻗어 키 2를 누른다(구 모델의 L4가 아님). 손가락 칩: `L1`.

**예제 6 — 도♯4 (연동 쌍)** (`[3,4,8,11,12,13]`)
- L4 목표 = 키 11 (우선순위), 키 12는 linked(점선). L4 칩은 1개만.

### 8.6 사운드 동기화

- `pointerdown`(숫자 버튼): 애니메이션 시작 + 사운드 attack.
- `pointerup` / `pointercancel`: 사운드 release(200ms 테일). 운지 표시는 유지.
- 사운드는 애니메이션 완료를 기다리지 않는다(즉시 발음 — 학습 리듬 보존).

---

## 9. 사운드 설계 (Web Audio)

### 9.1 신시사이저

클라리넷은 **홀수 배음이 지배적**(원통 폐관 특성)이므로 `PeriodicWave`로 근사한다.

```js
const ctx = new (window.AudioContext || window.webkitAudioContext)();
// 홀수 배음 진폭 (1,3,5,7,9차 중심) — 부드러운 클라리넷풍
const real = new Float32Array([0, 1, 0, 0.55, 0, 0.30, 0, 0.12, 0, 0.06]);
const imag = new Float32Array(real.length);
const wave = ctx.createPeriodicWave(real, imag, { disableNormalization: false });

function play(midi) {                       // midi = "실음" 또는 설정에 따라 기보음
  const f = 440 * Math.pow(2, (midi - 69) / 12);
  const osc = ctx.createOscillator();  osc.setPeriodicWave(wave); osc.frequency.value = f;
  const gain = ctx.createGain();
  gain.gain.setValueAtTime(0, ctx.currentTime);
  gain.gain.linearRampToValueAtTime(0.28, ctx.currentTime + 0.04);   // attack 40ms
  const lp = ctx.createBiquadFilter(); lp.type = "lowpass"; lp.frequency.value = Math.min(f * 6, 8000);
  osc.connect(lp).connect(gain).connect(ctx.destination); osc.start();
  return { osc, gain };                       // stop 시 200ms 리니어 release 후 osc.stop()
}
```

- 재생 음높이: 기본 = **실음**(악기 설정 반영: B♭ −2 / A −3). 설정에서 기보음 재생 선택 가능.
- 동시 발음 1개(단선율 악기): 새 발음 시 이전 노트는 60ms 페이드 아웃.

### 9.2 iOS 오디오 잠금 해제

iOS Safari는 사용자 제스처 안에서만 `AudioContext`를 시작할 수 있다.
→ **첫 `pointerdown`에서 `ctx.resume()`** 호출. 소리 설정이 꺼져 있으면 생성 자체를 지연.

---

## 10. 상태 관리와 URL

### 10.1 상태 객체 (단일 출처)

```js
const state = {
  noteIndex: null,          // 0..41 | null(시작 화면: 운지 없음·전체 열림)
  inputAcc: "natural",      // 마지막 입력의 변화표 (표시용, §5.4)
  useAlternate: false,      // 대체 운지 보기
  octave: 4, accidental: "natural",
  instrument: "Bb",         // "Bb" | "A"
  labelMode: "both", sound: true, soundWritten: false,
  showHands: true, showLabels: true,
  speed: 1,                 // 1 | 0.5 | 0 (즉시)
};
```

- 렌더 흐름: `input → reduce(state) → render(정보 패널·키패드 disabled·칩) + animator.transitionTo(pattern)`.
- `pattern` 결정: `note[useAlternate && note.alternate ? "alternate" : "primary"]`.
- 대체 운지 스위치 전환 시 **기본↔대체 간 diff 애니메이션**을 재생한다(같은 음의 두 운지를
  비교 학습하는 핵심 기능).
- 옥타브·변화표 변경은 다음 입력부터 적용(현재 운지 유지). 악기 변경은 실음 표시·소리만 갱신.
- **시작 상태(noteIndex = null)**: 장면은 전체 열림(운지 없음)·RT 지지 유지. 정보 패널 제목은
  안내 문구 `"숫자 버튼을 눌러 계이름을 선택하세요"`, 음역 배지 비표시, 실음 줄은 `"—"`,
  손가락 칩 영역은 `"모든 구멍 열림"` 1개, [기본|대체] 스위치는 비활성. 장면 SVG의 초기
  `aria-label`은 `"클라리넷과 양손, 선택된 음 없음, 모든 구멍 열림"`, `aria-live` 초기 문구는
  안내 문구와 동일.
- **대체 운지 스위치 규칙**: 현재 음에 `alternate`가 없거나(23개 음) noteIndex가 null이면
  스위치를 `aria-disabled` + [기본] 위치로 비활성 표시(불투명도 0.35, §5.3과 동일)하되
  `state.useAlternate` 값은 **변경하지 않고 유지**한다 — 대체 있는 음으로 이동하면 스위치가
  복원되고 useAlternate=true였다면 대체 운지가 표시된다. 비활성 스위치 탭 시 토스트
  `"이 음은 대체 운지가 없어요"`. URL `alt=1` 복원 시 해당 음에 대체가 없어도 값은 true로
  복원하고 비활성 표시만 적용한다.

### 10.2 URL 해시 (GPT56 호환 확장)

```text
#note=Fs4&instrument=Bb&alt=1
```

- 로드 시 복원, 상태 변경 시 `history.replaceState`. `note`가 없으면 시작 화면.

### 10.3 설정 영속화

- 설정 시트 항목(`instrument`·`labelMode`·`sound`·`soundWritten`·`showHands`·`showLabels`·
  `speed`)은 변경 즉시 localStorage 키 `clarinet-fingering-settings`(JSON 직렬화)에 저장하고
  로드 시 복원한다.
- URL 해시의 `instrument`는 공유 링크 재현용 — 해시에 값이 있으면 localStorage보다 우선한다.
- `note`·`alt`는 학습 위치이므로 해시로만 관리한다(저장하지 않음).
- `file://` 등 localStorage 접근이 막힌 환경에서는 조용히 세션 한정으로 폴백한다(오류 표출 금지).

---

## 11. 접근성

- 숫자 버튼: `<button>` + `aria-label="1 도"` (변화표 반영 시 "1 도샤프").
  `aria-keyshortcuts` 명시. 비활성은 `aria-disabled` + 탭 시 토스트로 이유 고지.
- 토글(대체 운지·소리·손 가이드): `aria-pressed`.
- 정보 패널: `aria-live="polite"` — "도4, 씨4. 샬뤼모. 왼손 엄지, 검지, 중지, 약지 사용" 형식.
- 장면 SVG: `role="img"` + 동적 `aria-label`(현재 음·사용 손가락 요약).
- 색 이외 정보: 활성 키는 채움+외곽선 굵기, 활성 손가락은 접촉점·거리로도 구분(§7.2).
  흑백에서도 판독 가능해야 함.
- 대비: 본문 WCAG AA. 포커스 링 `:focus-visible` 2px.
- `prefers-reduced-motion` 대응(§8.2). 애니메이션 속도 "즉시" 옵션 제공.
- 터치 타깃 최소 44×44px, 권장 48px (숫자 버튼 46×64px — 최소 축 46px로 충족.
  §6.1의 344px 미만 축소 시에도 44px 유지).

---

## 12. 기술 스택과 파일 구조

### 12.1 스택

- **Vanilla JS + 단일 HTML 파일**(빌드 없음, 의존성 0). 소스 전통과 불변 규칙 §3-6·7 준수.
- ES2020 문법 허용(옵셔널 체이닝 등). 지원 브라우저: iOS Safari 15+, Chrome/Edge/Samsung
  Internet 최근 2년, `file://` 실행 포함.

### 12.2 단일 파일 내부 구획 (주석 배너로 구분)

```text
clarinet_solfege_trainer.html
├─ <style>            디자인 토큰·레이아웃·키 채움 transition·reduced-motion
└─ <script>
   ├─ [1] DATA        COMPONENTS · ROUND_IDS · HORIZONTAL_IDS · DATA · REGISTER (원본 추출 붙여넣기)
   ├─ [2] THEORY      SOLFEGE_KO · midiFromButton · displayFor · soundingFor
   │                  · midiName(flat 선호 인자 — §4.4.2, GPT56 샤프 전용판 재사용 금지)
   ├─ [3] FINGER      FINGER_KEYS · PRIORITY · REST · activeFingers · fingerTarget
   │                  · fingerStateMap · keyRoles
   ├─ [4] SCENE       defs · clarinetBase · palms · finger 노드 생성 · setFingerTip · setKeyState
   ├─ [5] ANIMATOR    diffTransition · timeline builder · rAF tick · interrupt
   ├─ [6] AUDIO       PeriodicWave synth · play/stop · iOS unlock
   ├─ [7] UI          키패드 렌더·disabled 계산 · 정보 패널 · 설정 시트 · 토스트
   ├─ [8] STATE       state · reduce · URL hash · 이벤트 바인딩
   └─ [9] BOOT        데이터 어설션(§14.2) → 장면 1회 생성 → 초기 렌더
```

### 12.3 보조 파일 (저장소용, 배포 대상 아님)

```text
tests/validate-data.mjs   원본 HTML에서 상수 추출 → 앱 파일 상수와 deepStrictEqual 비교
tests/keypad-coverage.mjs 42음 도달성·disabled 규칙 검증 (§14.2와 동일 로직을 Node로)
README.md                 실행법·검증법·데이터 출처
```

---

## 13. 구현 로드맵

| Phase | 내용 | 완료 조건 (DoD) |
|---|---|---|
| **0. 데이터 잠금** | 원본 HTML에서 상수 5종 추출, JSON 직렬화, 어설션 통과 후 앱 파일에 삽입 | §14.1 어설션 전부 통과. 실패 시 다음 단계 착수 금지 |
| **1. 정적 장면** | §7 구조로 장면 1회 생성. 프로토 4음(미3·솔4·시4·도♯6)을 하드코딩 전환(무애니메이션)으로 렌더 | 4음 육안 검사: 손가락 위치 자연스러움, 키 2=L1 확인, linked 키 점선 확인 |
| **2. 키패드·상태** | §5 매핑, disabled 계산, §10 상태·URL, 정보 패널 | §14.2 도달성 어설션 통과. 42음 전부 탭으로 도달 |
| **3. 애니메이션 엔진** | §8 diff·타임라인·rAF·인터럽트·reduced-motion | §8.5 예제 1–6 전 시나리오 육안+콘솔 검증, 연타 끊김 없음 |
| **4. 사운드** | §9 신시사이저, pointerdown/up 연동, iOS unlock | iPhone 실기에서 첫 탭에 발음, 실음/기보음 옵션 동작 |
| **5. 마감** | 설정 시트, 물리 키보드 단축키(§6.5), 설정 영속화(§10.3), 접근성 마무리, 가로 모드, 성능 점검, 흑백 판독성 | §14.3 체크리스트 전 항목 통과 |
| **6. 확장(선택)** | §16 퀴즈·멜로디 모드 | 별도 정의 |

---

## 14. 테스트와 검증

### 14.1 데이터 어설션 (Phase 0, 부팅 시에도 dev 플래그로 실행)

```js
assert(DATA.length === 42);
assert(DATA.filter(n => n.alternate).length === 19);
assert(DATA[0].midi === 52 && DATA.at(-1).midi === 93);
assert(DATA.every((n, i) => n.midi === 52 + i));
assert(COMPONENTS.length === 25);
assert(DATA.every(n => n.primary.every(k => Number.isInteger(k) && k >= 0 && k <= 24)));
assert(new Set(DATA.map(n => n.id)).size === 42);
// 회귀: tests/validate-data.mjs 에서 원본 파일과 deepStrictEqual
```

### 14.2 키패드·손가락 모델 어설션

```js
// 도달성: 42음 전부 최소 1개 (octave, button, acc) 조합 존재
const reachable = new Set();
for (const oct of [3,4,5,6]) for (const btn of [1,...,7]) for (const acc of ["flat","natural","sharp"]) {
  if (!validCombo(oct, btn, acc)) continue;         // §5.3 규칙
  const m = midiFromButton(oct, btn, acc);
  if (m >= 52 && m <= 93) reachable.add(m);
}
assert(reachable.size === 42);

// 손가락 모델 무결성
assert(activeFingers([]).length === 0);                                   // 솔4
assert(activeFingers([2]).join() === "L1");                               // 솔♯4 재배정
assert(activeFingers([3,4,8,13,15,17,18,20,24]).join() === "LT,L1,L2,L3,L4,R1,R2,R3"); // 미3: R4 미집계
assert(activeFingers([3,4,8,13,17,18,20,24]).includes("R4"));             // 파3: 24 단독 → R4
for (const n of DATA) { activeFingers(n.primary); if (n.alternate) activeFingers(n.alternate); } // no-throw

// 직접/연동 판정 (§4.3.5)
assert(keyRoles([3,4,8,13,17,19]).get(19) === "direct");          // 시3 대체: R2가 슬리버 직접 누름
assert(keyRoles([3,4,5,7,8]).get(7)  === "direct");               // 사이드 키 쌍 5+7 동시 누름
assert(keyRoles([3,9,10]).get(9)     === "direct");               // 왼손 레버 쌍 9+10 동시 누름
assert(keyRoles([3,4,8,11,12,13]).get(12) === "linked");          // 11→12 기계 연동
assert(keyRoles([3,4,8,13,15,17,18,20,24]).get(24) === "linked"); // 15→24 기계 연동
// 전수 검증: 61개 패턴에서 linked로 판정되는 키는 12 또는 24뿐이어야 한다
for (const n of DATA) for (const p of [n.primary, n.alternate].filter(Boolean))
  for (const [k, role] of keyRoles(p)) assert(role === "direct" || k === 12 || k === 24);
```

### 14.3 최종 체크리스트

**기능**
- [ ] 42음 전부 키패드 도달, 표시 이름·실음·음역 정확 (실음은 플랫 표기: 도4 → 시♭3(B♭3))
- [ ] 시작 화면(해시 없음)이 §10.1 시작 상태대로 렌더
- [ ] 대체 운지 19음에서 기본↔대체 전환 애니메이션 동작
- [ ] 대체 없는 23음에서 [기본|대체] 스위치 비활성 표시 + `useAlternate` 값 유지
- [ ] §8.5 예제 1–6 시나리오 통과
- [ ] 연타 인터럽트 시 손가락 점프 없음
- [ ] URL 해시 저장·복원, 설정 localStorage 저장·복원(§10.3)

**품질**
- [ ] 콘솔 오류 0, 네트워크 요청 0, `file://` 실행 정상
- [ ] 360×740 세로·가로 모두 가로 스크롤 없음
- [ ] 중급 Android(예: Galaxy A 시리즈)에서 전환 애니메이션 프레임 드랍 없음
- [ ] `prefers-reduced-motion` 즉시 모드 동작
- [ ] iOS 첫 탭 발음(오디오 잠금 해제), 무음 모드 스위치 문서화

**접근성**
- [ ] §11 전 항목 (aria-live·aria-pressed·키보드·대비·흑백 판독)

**데이터**
- [ ] `tests/validate-data.mjs` diff 0

### 14.4 기기 매트릭스

| 기기 | 브라우저 | 필수 확인 |
|---|---|---|
| iPhone (최근 3년) | Safari | 오디오 unlock, 100dvh, safe-area |
| Android 중급기 | Chrome | 60fps, 터치 지연 |
| Android | Samsung Internet | 폰트·이모지 렌더 |
| 데스크톱 | Chrome/Edge | 물리 키보드 단축키, 리사이즈 |

---

## 15. 완료 기준 (Definition of Done)

1. §14.3 체크리스트 전 항목 통과.
2. 운지 데이터가 원본과 프로그램 비교로 완전 일치(diff 0).
3. 강사(사용자) 시연 검수: 예제 2(브레이크 통과)와 예제 5(키 2 = L1)를 실연주 감각과
   비교해 승인.
4. 단일 HTML 파일 1개 + 보조 테스트 스크립트 2개 + README 납품.

---

## 16. 확장 로드맵 (v2 이후)

| 기능 | 개요 |
|---|---|
| 퀴즈 모드 | 계이름 텍스트(또는 소리)를 제시 → 학습자가 키패드로 응답 → 정오답 피드백·진행도(localStorage) |
| 멜로디 모드 | 내장 숫자보 시퀀스(비행기·나비야 등)를 한 음씩 진행하며 운지 관찰. BPM 조절 자동 재생 |
| 반음 탐색 강화 | ◀▶ 크로매틱 이동 버튼, `←/→` 키, 스와이프 제스처 (§6.5에서 이관) |
| 트릴/연결 학습 | 두 음을 지정해 반복 전환 애니메이션(속도 슬라이더) |
| PWA 패키징 | 단일 파일 원칙과 별도로 manifest+SW를 둔 설치형 빌드(별도 산출물) |
| 다크 모드 | `prefers-color-scheme` + 수동 토글 |

---

## 부록 A. 42음 × 키패드 매핑 표

옥타브 버튼 = 기보 옥타브. `[n]♯` = 숫자 n + ♯ 토글. 이명동음 입력은 동일 음의 대안 조합.

### 옥타브 3 (버튼 1·2 비활성, 미♭3 비활성)

| MIDI | 기보음 | 계이름 | id | 음역 | 입력 | 이명동음 입력 | 대체 운지 |
|---|---|---|---|---|---|---|---|
| 52 | E3 | 미3 | E3 | 샬뤼모 | `[3]♮` | — | ✔ |
| 53 | F3 | 파3 | F3 | 샬뤼모 | `[4]♮` | — | ✔ |
| 54 | F♯3 | 파♯3 | Fs3 | 샬뤼모 | `[4]♯` | `[5]♭` 솔♭3 | ✔ |
| 55 | G3 | 솔3 | G3 | 샬뤼모 | `[5]♮` | — | — |
| 56 | G♯3 | 솔♯3 | Gs3 | 샬뤼모 | `[5]♯` | `[6]♭` 라♭3 | — |
| 57 | A3 | 라3 | A3 | 샬뤼모 | `[6]♮` | — | — |
| 58 | A♯3 | 라♯3 | As3 | 샬뤼모 | `[6]♯` | `[7]♭` 시♭3 | — |
| 59 | B3 | 시3 | B3 | 샬뤼모 | `[7]♮` | — | ✔ |

### 옥타브 4 (12음 전부 유효)

| MIDI | 기보음 | 계이름 | id | 음역 | 입력 | 이명동음 입력 | 대체 운지 |
|---|---|---|---|---|---|---|---|
| 60 | C4 | 도4 | C4 | 샬뤼모 | `[1]♮` | — | — |
| 61 | C♯4 | 도♯4 | Cs4 | 샬뤼모 | `[1]♯` | `[2]♭` 레♭4 | — |
| 62 | D4 | 레4 | D4 | 샬뤼모 | `[2]♮` | — | — |
| 63 | D♯4 | 레♯4 | Ds4 | 샬뤼모 | `[2]♯` | `[3]♭` 미♭4 | ✔ |
| 64 | E4 | 미4 | E4 | 샬뤼모 | `[3]♮` | — | — |
| 65 | F4 | 파4 | F4 | 샬뤼모 | `[4]♮` | — | — |
| 66 | F♯4 | 파♯4 | Fs4 | 샬뤼모 | `[4]♯` | `[5]♭` 솔♭4 | ✔ |
| 67 | G4 | 솔4 | G4 | 목음역 | `[5]♮` | — | — |
| 68 | G♯4 | 솔♯4 | Gs4 | 목음역 | `[5]♯` | `[6]♭` 라♭4 | — |
| 69 | A4 | 라4 | A4 | 목음역 | `[6]♮` | — | — |
| 70 | A♯4 | 라♯4 | As4 | 목음역 | `[6]♯` | `[7]♭` 시♭4 | ✔ |
| 71 | B4 | 시4 | B4 | 클라리온 | `[7]♮` | — | ✔ |

### 옥타브 5 (12음 전부 유효)

| MIDI | 기보음 | 계이름 | id | 음역 | 입력 | 이명동음 입력 | 대체 운지 |
|---|---|---|---|---|---|---|---|
| 72 | C5 | 도5 | C5 | 클라리온 | `[1]♮` | — | ✔ |
| 73 | C♯5 | 도♯5 | Cs5 | 클라리온 | `[1]♯` | `[2]♭` 레♭5 | ✔ |
| 74 | D5 | 레5 | D5 | 클라리온 | `[2]♮` | — | — |
| 75 | D♯5 | 레♯5 | Ds5 | 클라리온 | `[2]♯` | `[3]♭` 미♭5 | — |
| 76 | E5 | 미5 | E5 | 클라리온 | `[3]♮` | — | — |
| 77 | F5 | 파5 | F5 | 클라리온 | `[4]♮` | — | — |
| 78 | F♯5 | 파♯5 | Fs5 | 클라리온 | `[4]♯` | `[5]♭` 솔♭5 | ✔ |
| 79 | G5 | 솔5 | G5 | 클라리온 | `[5]♮` | — | — |
| 80 | G♯5 | 솔♯5 | Gs5 | 클라리온 | `[5]♯` | `[6]♭` 라♭5 | — |
| 81 | A5 | 라5 | A5 | 클라리온 | `[6]♮` | — | — |
| 82 | A♯5 | 라♯5 | As5 | 클라리온 | `[6]♯` | `[7]♭` 시♭5 | ✔ |
| 83 | B5 | 시5 | B5 | 클라리온 | `[7]♮` | — | — |

### 옥타브 6 (버튼 7 비활성, 라♯6/시♭6 비활성)

| MIDI | 기보음 | 계이름 | id | 음역 | 입력 | 이명동음 입력 | 대체 운지 |
|---|---|---|---|---|---|---|---|
| 84 | C6 | 도6 | C6 | 클라리온 | `[1]♮` | — | — |
| 85 | C♯6 | 도♯6 | Cs6 | 알티시모 | `[1]♯` | `[2]♭` 레♭6 | ✔ |
| 86 | D6 | 레6 | D6 | 알티시모 | `[2]♮` | — | ✔ |
| 87 | D♯6 | 레♯6 | Ds6 | 알티시모 | `[2]♯` | `[3]♭` 미♭6 | ✔ |
| 88 | E6 | 미6 | E6 | 알티시모 | `[3]♮` | — | — |
| 89 | F6 | 파6 | F6 | 알티시모 | `[4]♮` | — | ✔ |
| 90 | F♯6 | 파♯6 | Fs6 | 알티시모 | `[4]♯` | `[5]♭` 솔♭6 | ✔ |
| 91 | G6 | 솔6 | G6 | 알티시모 | `[5]♮` | — | ✔ |
| 92 | G♯6 | 솔♯6 | Gs6 | 알티시모 | `[5]♯` | `[6]♭` 라♭6 | ✔ |
| 93 | A6 | 라6 | A6 | 알티시모 | `[6]♮` | — | — |

합계: 8 + 12 + 12 + 10 = **42음** · 대체 운지 ✔ = **19개**

---

## 부록 B. 키 ID 의미 표 (25개, 추상 키맵 기준)

> 명칭은 데이터 사용례에서 유도한 교육용 라벨이다. 실제 악기의 키 명칭·형상은 제조사별로
> 다를 수 있다. 형태: ●=원형(`ROUND_IDS`), ▬=가로 사각(`HORIZONTAL_IDS`), ▮=세로 사각.

| ID | 형태 | 위치(y) | 교육용 명칭 | 손가락 | 주요 사용례 (DATA) |
|---|---|---|---|---|---|
| 0 | ▮ | −12.4 | A 키 (목음역) | L1 | 라4 `[0]`, 시♭4 `[0,1]` |
| 1 | ▮ | −11.3 | 레지스터 키 | LT | 클라리온·알티시모 전부 |
| 2 | ▬ | −9.4 | 목음역 G♯/A♭ 키 | **L1** (재배정 §4.3.1) | 솔♯4 `[2]` |
| 3 | ● | 0.0 | 엄지 구멍 | LT | 대부분의 음 |
| 4 | ● | 0.5 | 왼손 검지 구멍 | L1 | 대부분의 음 |
| 5 | ● | 8.9 | 사이드 E♭/B♭ 키 A | R1 | 레♯4 대체, 라♯5 대체 |
| 6 | ▮ | 9.7 | 사이드 트릴 키 | R1 | 시♭4 대체 `[0,6]`, 레6 대체 `[1,3,6]` |
| 7 | ▬ | 9.8 | 사이드 E♭/B♭ 키 B | R1 | 레♯4 대체, 라♯5 대체 (5와 쌍) |
| 8 | ● | 9.9 | 왼손 중지 구멍 | L2 | 대부분의 음 |
| 9 | ▮ | 13.7 | 왼손 측면 레버 A | L4 (10과 동시 누름 → direct) | 파♯4 대체 `[3,9,10]`, 도♯6 대체 |
| 10 | ▮ | 15.9 | 왼손 E♭/B♭ 레버 | L4 | 레♯4 `[3,4,8,10]`, 라♯5 `[1,3,4,8,10]` |
| 11 | ● | 15.9 | C♯/G♯ 레버 | L4 | 도♯4·솔♯5·파6 |
| 12 | ▬ | 16.8 | C♯/G♯ 패드 (11과 연동) | (L4) | 11과 항상 동반 → linked 표시 |
| 13 | ● | 17.6 | 왼손 약지 구멍 | L3 | 대부분의 음 |
| 14 | ▬ | 22.9 | 왼손 F/C 레버 | L4 | 파3 대체, 도5 대체 |
| 15 | ▮ | 24.9 | 왼손 E/B 레버 | L4 | 미3 `…15…24`, 시4 (24와 연동) |
| 16 | ▮ | 26.7 | 왼손 F♯/C♯ 레버 | L4 | 파♯3, 도♯5, 솔♯6 대체, 라6 |
| 17 | ● | 32.4 | 오른손 검지 구멍 | R1 | 대부분의 음 |
| 18 | ● | 38.9 | 오른손 중지 구멍 | R2 | 대부분의 음 |
| 19 | ▬ | 44.4 | 슬리버 키 | **R2** (재배정 §4.3.1) | 시3 대체, 파♯5 대체, 레♯6, 솔♯6 |
| 20 | ● | 47.0 | 오른손 약지 구멍 | R3 | 대부분의 음 |
| 21 | ▮ | 52.0 | 오른손 F♯/C♯ 레버 | R4 | 파♯3 대체, 도♯5 대체 |
| 22 | ▮ | 53.1 | 오른손 A♭/E♭ 레버 | R4 | 솔♯3, 레♯5, 알티시모 다수 |
| 23 | ▮ | 56.1 | 오른손 E/B 레버 | R4 | 미3 대체, 시4 대체 |
| 24 | ▮ | 57.1 | 오른손 F/C 레버·패드 | R4 (15/16 동반 시 linked) | 파3·도5 직접, 미3·시4 연동 |

---

## 부록 C. 전환 diff 손가락 분류 요약

| 분류 | 조건 | 모션 |
|---|---|---|
| PRESS | 비활성 → 활성 | 대기점 → 키 목표점 이동 후 누름 (임팩트·halo) |
| RELEASE | 활성 → 비활성 | 키 목표점 → 대기점으로 들어 올림 |
| SLIDE | 활성 유지 + 담당 키 변경 | 살짝 들기 → 이동 → 누름 |
| HOLD | 활성 유지 + 담당 키 동일 | 모션 없음 (신규 담당 키 채움만 가능) |
| IDLE | 비활성 유지 | 모션 없음 |

## 부록 D. 용어집

| 용어 | 정의 |
|---|---|
| 기보음 | 악보에 적힌 음. 운지·키패드의 기준 |
| 실음 | 실제 울리는 음. B♭ 악기는 장2도, A 악기는 단3도 아래 |
| 고정도법 | "도 = C" 고정 계이름 체계. 본 앱의 계이름 표기 방식 |
| 샬뤼모/목음역/클라리온/알티시모 | 클라리넷 음역 4구분 (§4.1) |
| 브레이크(the break) | 목음역 ↔ 클라리온 경계(시♭4→시4). 손가락이 가장 크게 재배치되는 구간 |
| 직접 접촉 키 / 연동 키 | 손가락이 직접 누르는 키 / 기계 연동으로 닫히는 키 (§4.3.5) |
| 대기점(REST) | 비활성 손가락 끝이 머무는 고정 좌표 (§4.3.4) |
