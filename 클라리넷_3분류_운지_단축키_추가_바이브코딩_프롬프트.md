# 클라리넷 낮은음·높은음·반음 운지 애니메이션 추가용 바이브코딩 마스터 프롬프트

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.0 (2026-07-19) |
| 기준 개발서 | `클라리넷_계이름_운지법_애니메이션_웹앱_개발서.md` v1.1 |
| 대상 앱 | `clarinet_solfege_trainer.html` |
| 작업 성격 | 기존 42음 운지 데이터와 diff 애니메이터를 재사용하는 **입력·분류 UI 확장** |
| 핵심 범위 | C4–B4 자연음, C5–B5 자연음, 두 옥타브의 변화음 10개 |

---

## 0. 이 프롬프트의 사용법

코딩 에이전트에게 다음 자료를 한 폴더에 함께 제공하고, 이 문서의 **1장 마스터 지시문부터 마지막 완료 보고까지 순서대로 실행**하게 한다.

```text
클라리넷_계이름_운지법_애니메이션_웹앱_개발서.md
클라리넷_3분류_운지_단축키_추가_바이브코딩_프롬프트.md
01 Source/
```

현재 작업 폴더에 `clarinet_solfege_trainer.html`이 없을 수 있다.

- 파일이 **있으면** 현재 동작을 기준선으로 잡고 비파괴적으로 확장한다.
- 파일이 **없으면** 별도의 축약 데모를 만들지 말고, 기준 개발서의 Phase 0–5를 따라 기반 앱을 구현하면서 이 기능을 같은 파일에 통합한다.
- 어느 경우든 계획이나 코드 조각만 제시하고 끝내지 않는다. 실제 구현, 자동 검증, 브라우저 확인, README 갱신, 완료 보고까지 수행한다.

---

## 1. 마스터 지시문

당신은 다음 역할을 동시에 수행한다.

1. 표준 보엠식 클라리넷 운지 데이터의 정합성을 지키는 음악 교육 앱 엔지니어
2. 상태 기반 Vanilla JavaScript 웹앱을 설계하는 시니어 프런트엔드 개발자
3. 영속 SVG 노드와 `requestAnimationFrame` diff 애니메이션을 다루는 인터랙션 엔지니어
4. 키보드·터치·스크린리더 입력을 함께 검증하는 접근성 엔지니어
5. 원본 데이터 회귀, 단축키 매핑, 실제 브라우저 제약을 구분해 보고하는 QA 엔지니어

### 1.1 최종 목표

기준 개발서가 정의한 E3–A6 기보음 42개, 기본 운지 42개, 대체 운지 19개, 손가락 diff 애니메이션, 사운드, 정보 패널, URL 상태를 그대로 보존하면서 다음 세 교육용 빠른 분류를 추가한다.

1. **낮은 계이름**: 가온 도 C4에서 시작하는 자연음 C4–B4
2. **높은 계이름**: 높은 도 C5에서 시작하는 자연음 C5–B5
3. **반음(변화음)**: 위 두 옥타브에 있는 C♯, D♯, F♯, G♯, A♯ 10개

물리 키보드의 주 입력 계약은 다음과 같다.

```text
Control + 숫자 1–7  → 낮은 계이름 C4–B4
Shift   + 숫자 1–7  → 높은 계이름 C5–B5
Alt     + 숫자       → 현재 빠른 음역(C4 또는 C5)의 반음(변화음)
```

모바일·터치 환경과 브라우저 예약 단축키 충돌 환경을 위해 같은 기능을 수행하는 화면상의 3분류 선택 UI를 반드시 함께 제공한다.

### 1.2 이 기능의 정확한 성격

기준 `DATA`에는 E3–A6의 모든 반음계 음과 각 음의 운지가 이미 존재한다. 따라서 이 작업에서 새로 만드는 것은 **운지 데이터가 아니라 선택 경로**다.

반드시 다음 한 경로를 재사용한다.

```text
빠른 분류 버튼/단축키
  → MIDI 선택 요청
  → 기존 NOTE_BY_MIDI의 note 객체
  → 기존 reducer/state
  → 기존 render + URL + aria-live
  → 기존 primary/alternate pattern 결정
  → 기존 animator.transitionTo(pattern)
  → 기존 Web Audio attack/release
```

음별 `primary` 배열, 손가락 목록, SVG 마크업, 별도 애니메이션을 새 분류 안에 복제하지 않는다.

---

## 2. 기준 자료의 우선순위와 분석 결과

### 2.1 우선순위

충돌이 생기면 다음 순서로 판단한다.

1. `01 Source/clarinet_standard_fingering_chart.html`의 운지·키 좌표 데이터
2. `클라리넷_계이름_운지법_애니메이션_웹앱_개발서.md`의 상태·손가락·애니메이션·접근성 규칙
3. 이 문서의 3분류 매핑과 단축키 규칙
4. 기존 구현의 표현 세부사항

이 문서는 입력 계층에 한해서만 기준 개발서를 확장한다. 원본 운지 데이터와 손가락 접촉 규칙을 덮어쓸 권한은 없다.

### 2.2 기존 설계에서 그대로 재사용할 항목

- 기보 MIDI 52–93의 연속 42음
- `NOTE_BY_MIDI = new Map(DATA.map(n => [n.midi, n]))`
- `midiFromButton(octave, button, accidental)`
- `displayFor`, `soundingFor`, `midiName`
- `activeFingers`, `fingerTarget`, `fingerStateMap`, `keyRoles`
- PRESS·RELEASE·SLIDE·HOLD·IDLE를 계산하는 `diffTransition`
- 연타 중 현재 보간 좌표에서 다시 시작하는 인터럽트 정책
- primary/alternate 선택과 `useAlternate` 유지 정책
- `pointerdown` attack, `pointerup/pointercancel` release 정책
- 정보 패널, URL 해시, localStorage, reduced-motion 규칙
- 원본 diff 0 및 기존 42음 키패드 도달성 테스트

### 2.3 교육용 분류와 악기 음역을 혼동하지 말 것

`낮은 계이름`, `높은 계이름`, `반음`은 새 입력 UI의 교육용 분류다. 기존 `register` 또는 `registerKo` 값을 바꾸지 않는다.

예를 들어 낮은 계이름 그룹 C4–B4는 샬뤼모·목음역·클라리온을 가로지른다. `Control+7`로 선택한 B4는 여전히 `클라리온`으로 표시되어야 한다.

---

## 3. 절대 변경 금지 규칙

1. `01 Source/`의 모든 파일은 읽기 전용이다.
2. 원본 `COMPONENTS`, `ROUND_IDS`, `HORIZONTAL_IDS`, `DATA`, `REGISTER`를 수정하지 않는다.
3. `DATA`의 순서, MIDI, id, `primary`, `alternate`, `register`, `registerKo`를 수정하지 않는다.
4. 새 분류용 운지 배열이나 손가락 배열을 만들지 않는다.
5. 샤프와 플랫 이명동음을 서로 다른 운지로 복제하지 않는다.
6. 키 ID를 눈으로 재입력하거나 외부 운지표의 값으로 덮어쓰지 않는다.
7. 기존 42음 전체 선택 UI를 제거하거나 C4–B5로 축소하지 않는다.
8. 기존 일반 숫자 `1–7`, `↑/↓`, `+/-`, 터치 입력을 삭제하지 않는다.
9. 장면 SVG DOM을 음 선택마다 재생성하지 않는다. 영속 노드의 속성·클래스만 갱신한다.
10. 단축키 전용 애니메이션 엔진이나 오디오 엔진을 따로 만들지 않는다.
11. 오른손 엄지 RT, 키 2→L1, 키 19→R2, 키 24 연동 예외를 변경하지 않는다.
12. 대체 운지가 없는 음에 대체 운지를 추가하지 않는다.
13. 대체 운지 선택 상태 `useAlternate`를 단축키 입력 때 초기화하지 않는다.
14. 외부 CDN·폰트·라이브러리·이미지·네트워크 요청을 추가하지 않는다.
15. `file://` 더블클릭 실행과 단일 배포 HTML 원칙을 유지한다.
16. `prefers-reduced-motion: reduce`와 즉시 모드를 우회하지 않는다.
17. 브라우저가 예약한 키 조합을 JavaScript로 강제 탈취할 수 있다고 가정하거나, 실기 확인 없이 성공했다고 보고하지 않는다.

---

## 4. 세 분류와 단축키의 확정 명세

### 4.1 공통 음높이 기준

- 모든 음, 운지, 단축키, 정보 패널의 기준은 **클라리넷 기보음**이다.
- C4는 기보 MIDI 60이다.
- B♭ 클라리넷을 선택하면 기보 C4의 실음은 B♭3이지만, 단축키는 여전히 C4를 선택한다.
- 숫자의 의미는 기존 숫자보와 동일하게 `1=도, 2=레, 3=미, 4=파, 5=솔, 6=라, 7=시`다.

### 4.2 낮은 계이름: Control+1–7

| 숫자 | 단축키 | 계이름 | 영문 id | MIDI | accidental/inputAcc |
|---:|---|---|---|---:|---|
| 1 | `Control+1` | 도4 | C4 | 60 | natural |
| 2 | `Control+2` | 레4 | D4 | 62 | natural |
| 3 | `Control+3` | 미4 | E4 | 64 | natural |
| 4 | `Control+4` | 파4 | F4 | 65 | natural |
| 5 | `Control+5` | 솔4 | G4 | 67 | natural |
| 6 | `Control+6` | 라4 | A4 | 69 | natural |
| 7 | `Control+7` | 시4 | B4 | 71 | natural |

유효 입력 시 `quickBand=4`, `state.octave=4`, `state.accidental="natural"`, `state.inputAcc="natural"`로 동기화한다.

### 4.3 높은 계이름: Shift+1–7

| 숫자 | 단축키 | 계이름 | 영문 id | MIDI | accidental/inputAcc |
|---:|---|---|---|---:|---|
| 1 | `Shift+1` | 도5 | C5 | 72 | natural |
| 2 | `Shift+2` | 레5 | D5 | 74 | natural |
| 3 | `Shift+3` | 미5 | E5 | 76 | natural |
| 4 | `Shift+4` | 파5 | F5 | 77 | natural |
| 5 | `Shift+5` | 솔5 | G5 | 79 | natural |
| 6 | `Shift+6` | 라5 | A5 | 81 | natural |
| 7 | `Shift+7` | 시5 | B5 | 83 | natural |

유효 입력 시 `quickBand=5`, `state.octave=5`, `state.accidental="natural"`, `state.inputAcc="natural"`로 동기화한다.

### 4.4 반음(변화음): Alt+숫자

이 문서에서 화면 라벨 `반음`은 엄밀한 음정명이 아니라, 자연음 사이의 **변화음/반음계 음**을 뜻하는 교육용 축약어다.

반음 그룹은 숫자보의 도수를 유지한다. 유효 숫자는 `1, 2, 4, 5, 6`이며, `Alt+3`과 `Alt+7`을 다른 순번의 반음에 재배치하지 않는다.

| 숫자 | `quickBand=4` | MIDI | `quickBand=5` | MIDI | 결과 |
|---:|---|---:|---|---:|---|
| 1 | 도♯4 / 레♭4 · Cs4 | 61 | 도♯5 / 레♭5 · Cs5 | 73 | 유효 |
| 2 | 레♯4 / 미♭4 · Ds4 | 63 | 레♯5 / 미♭5 · Ds5 | 75 | 유효 |
| 3 | 미♯4 = 파4 | 65 | 미♯5 = 파5 | 77 | 비활성·토스트 |
| 4 | 파♯4 / 솔♭4 · Fs4 | 66 | 파♯5 / 솔♭5 · Fs5 | 78 | 유효 |
| 5 | 솔♯4 / 라♭4 · Gs4 | 68 | 솔♯5 / 라♭5 · Gs5 | 80 | 유효 |
| 6 | 라♯4 / 시♭4 · As4 | 70 | 라♯5 / 시♭5 · As5 | 82 | 유효 |
| 7 | 시♯4 = 도5 | 72 | 시♯5 = 도6 | 84 | 비활성·토스트 |

반음 입력의 세부 규칙:

1. 초기 `quickBand`는 4다.
2. 낮은 계이름 버튼 또는 `Control+숫자`를 사용하면 `quickBand=4`가 된다.
3. 높은 계이름 버튼 또는 `Shift+숫자`를 사용하면 `quickBand=5`가 된다.
4. 전체 음역 UI에서 옥타브 4 또는 5를 명시적으로 선택하면 `quickBand`도 그 값으로 갱신한다.
5. 전체 음역 UI에서 옥타브 3 또는 6을 선택해도 `quickBand`는 마지막 4/5 값을 유지한다.
6. `Alt+숫자`는 `quickBand`를 사용하며, 선택 시 `state.octave=quickBand`, `accidental/inputAcc="sharp"`로 동기화한다.
7. 샤프를 주 표기로 표시하고 DATA의 플랫 이명동음을 부제로 병기한다.
8. `Alt+3`, `Alt+7`은 현재 음, 운지, 해시, 사운드를 바꾸지 않고 기존 §5.3의 이명동음 토스트 생성기를 호출한다.
9. `Alt+3`, `Alt+7`을 각각 F와 다음 C로 자동 이동시키지 않는다.

### 4.5 24개 고유 목표음 보증

```text
LOW_NATURAL   = [60, 62, 64, 65, 67, 69, 71]
HIGH_NATURAL  = [72, 74, 76, 77, 79, 81, 83]
LOW_CHROMATIC = [61, 63, 66, 68, 70]
HIGH_CHROMATIC= [73, 75, 78, 80, 82]
```

네 집합의 합집합은 MIDI 60–83의 연속 24음이어야 한다. 중복 제거 후 정확히 24개이고, 모든 MIDI가 `NOTE_BY_MIDI`에 존재해야 한다.

### 4.6 범위 밖 음의 접근성

E3–B3와 C6–A6은 이 세 빠른 분류의 범위 밖이지만 기존 옥타브×숫자×변화표 UI로 계속 접근할 수 있어야 한다. 이 기능을 이유로 기존 42음 도달성 완료 기준을 낮추지 않는다.

---

## 5. 생성·수정할 파일

### 5.1 필수

```text
clarinet_solfege_trainer.html
tests/validate-data.mjs
tests/keypad-coverage.mjs
tests/keyboard-shortcuts.mjs
README.md
```

### 5.2 파일 처리 원칙

- 대상 HTML이 이미 있으면 전체를 새로 생성하지 말고 현재 구조와 사용자 변경을 보존하며 필요한 부분만 수정한다.
- 대상 HTML이 없으면 기준 개발서의 완성 기준을 충족하는 앱을 만든 뒤 이 기능까지 통합한다.
- 새 기능만 보여 주는 별도 `demo.html`, 두 번째 앱, 별도 운지 데이터 파일을 만들지 않는다.
- 배포 대상은 계속 `clarinet_solfege_trainer.html` 한 파일이다.
- 테스트 스크립트와 README는 저장소용 보조 파일이며 HTML의 `file://` 실행에 필요해서는 안 된다.

### 5.3 단일 HTML 내부 배치

기준 개발서의 주석 구획을 유지하며 다음 위치에 넣는다.

```text
[2] THEORY   TESTABLE_SHORTCUT_CORE marker 안의 PC · ACC · midiFromButton
             · SEMITONE_DEGREES · deriveQuickCategory · resolveQuickRequest
             · shortcutKindFromEvent(부작용 없는 parser)
[6] AUDIO    키보드 voice token을 받을 수 있는 기존 attack/release 보강
[7] UI       빠른 분류 3버튼 · 밴드 표시 · shortcut 도움말 · 렌더 함수
[8] STATE    quickBand · 공용 SELECT_NOTE_REQUEST action
             · 단일 전역 router · keydown/keyup/cleanup
[9] BOOT     매핑 어설션 · 전역 리스너 1회 등록
```

- `DATA` 구획에는 새 분류 상수를 넣지 않는다.
- `ANIMATOR` 구획에는 단축키 분기 코드를 넣지 않는다.
- UI가 animator 내부 함수를 직접 호출하지 않고 공용 dispatch 흐름을 통과하게 한다.

---

## 6. 상태와 공용 선택 파이프라인 설계

### 6.1 추가 상태

기존 `state`에 UI 전용 상태를 최소한으로 추가한다.

```js
const state = {
  // ...기준 개발서의 기존 필드...
  quickBand: 4, // 4 | 5
};
```

규칙:

- `quickBand`는 반음 단축키의 숨은 문맥이므로 화면에 항상 드러내야 한다.
- `quickBand`는 운지 데이터가 아니다.
- `quickCategory`를 별도의 변경 가능한 상태로 저장하지 않는다. 다음 순수 함수로 `state.octave`와 `state.accidental`에서 매 render마다 파생한다.

```js
function deriveQuickCategory(octave, accidental) {
  if (octave === 4 && accidental === "natural") return "low-natural";
  if (octave === 5 && accidental === "natural") return "high-natural";
  if ((octave === 4 || octave === 5) && accidental === "sharp") return "semitone";
  return null;
}
```

- 기존 전체 음역 컨트롤을 조작할 때 `quickBand`를 다음처럼 동기화한다.
  - octave=4 → `quickBand=4`
  - octave=5 → `quickBand=5`
  - octave=3/6 → 마지막 `quickBand` 보존
  - flat 여부는 band를 바꾸지 않으며 `deriveQuickCategory` 결과만 null이 된다.
- 따라서 화면의 빠른 분류 강조와 실제 기존 키패드가 서로 다른 옥타브·변화표를 가리키는 상태를 허용하지 않는다.
- URL 공유의 핵심은 선택된 `note`, `instrument`, `alt`이므로 새 필드를 URL 필수 파라미터로 만들지 않는다.
- URL로 음을 복원할 때도 같은 파생 규칙을 사용한다. C4–B4 자연음은 octave=4/natural/band4, C5–B5 자연음은 5/natural/band5, 그 두 옥타브의 샤프 id(`Cs`, `Ds`, `Fs`, `Gs`, `As`)는 해당 octave/sharp/band로 복원한다. 해시에 플랫 입력 선호가 따로 없으므로 변화음은 DATA의 샤프 primary 표기를 사용한다.
- URL 음이 범위 밖이면 `deriveQuickCategory(...)`가 null을 반환하되 전체 음역 UI는 정상 복원한다.
- localStorage에 저장한다면 버전 없는 별도 키를 만들지 말고 기존 설정 객체에 후방 호환 기본값을 둔다. 저장하지 않아도 무방하다.

### 6.2 순수 요청 모델

DOM, SVG, 오디오를 건드리지 않는 순수 함수부터 만든다.

```js
const NATURAL_DEGREES = Object.freeze([1, 2, 3, 4, 5, 6, 7]);
const SEMITONE_DEGREES = new Set([1, 2, 4, 5, 6]);

function resolveQuickRequest(kind, digit, quickBand) {
  if (!Number.isInteger(digit) || digit < 1 || digit > 7) return null;

  let octave;
  let accidental;

  if (kind === "low-natural") {
    octave = 4;
    accidental = "natural";
  } else if (kind === "high-natural") {
    octave = 5;
    accidental = "natural";
  } else if (kind === "semitone") {
    octave = quickBand;
    accidental = "sharp";
  } else {
    return null;
  }

  const midi = midiFromButton(octave, digit, accidental);
  const valid = kind !== "semitone" || SEMITONE_DEGREES.has(digit);

  return {
    kind,
    digit,
    octave,
    accidental,
    inputAcc: accidental,
    midi,
    valid,
  };
}
```

이 코드는 구조 예시다. 기존 앱의 함수명과 reducer 스타일에 맞춰 조정하되 다음 조건은 유지한다.

- 운지 키 ID가 등장하지 않는다.
- note 객체나 `primary/alternate` 배열을 복제하지 않는다.
- `midiFromButton`을 재사용한다.
- 유효성 판단과 상태 변경을 분리한다.
- 유효하지 않은 요청도 올바른 이명동음 토스트를 만들 수 있도록 octave, digit, midi를 보존한다.

### 6.3 하나의 선택 진입점

화면의 기존 숫자 버튼, 새 3분류 UI, 물리 단축키가 가능한 한 같은 선택 진입점을 사용하도록 리팩터링한다.

권장 계약:

```js
function selectNoteRequest(request, source) {
  // source: "legacy-pointer" | "quick-pointer" | "keyboard-shortcut"
  // 1) request 유효성 확인
  // 2) NOTE_BY_MIDI에서 기존 note 조회
  // 3) 단일 reducer action dispatch
  // 4) 한 번의 render 사이클
  // 5) 기존 pattern 결정
  // 6) animator.transitionTo(pattern)
  // 7) URL/aria-live 갱신
  // 8) { handled, note, midi } 반환 — 오디오 attack은 여기서 하지 않음
}
```

선택 상태·렌더·애니메이션과 입력 수명·오디오의 소유권을 다음처럼 분리한다.

```js
function beginNoteInput(request, { source, token }) {
  const result = selectNoteRequest(request, source); // dispatch·render·animator 각 1회
  if (!result.handled) return false;
  audioGate.attack(result.note, token);              // 유효 입력당 1회
  return true;
}

function endNoteInput(token) {
  audioGate.release(token);                          // 현재 voice token 일치 시에만
}
```

- `selectNoteRequest`는 note 선택과 animator까지 소유하지만 attack/release를 소유하지 않는다.
- 포인터와 키보드 adapter는 둘 다 `beginNoteInput`/`endNoteInput`을 사용한다.
- 분류 버튼처럼 note를 선택하지 않는 UI는 `beginNoteInput`을 호출하지 않는다.
- invalid Alt+3/7은 토스트 1회만 만들고 `selectNoteRequest`, animator, audioGate를 호출하지 않는다.
- 유효 keydown 또는 pointerdown 1회당 dispatch 1회, render 1회, animator 1회 이하, attack 1회여야 한다.

반드시 지킬 것:

- 유효한 요청인데 `NOTE_BY_MIDI.get(midi)`가 없으면 조용히 폴백하지 말고 개발 오류로 보고한다.
- `note.index`가 1부터 시작하고 `state.noteIndex`가 0부터 시작하는 차이를 처리한다.
- reducer 밖에서 여러 상태 필드를 따로 변경해 중간 렌더를 만들지 않는다.
- 단축키 입력 한 번에 정보 패널과 애니메이션을 중복 호출하지 않는다.
- 현재 `useAlternate=true`이고 새 음에 alternate가 있으면 alternate pattern을 사용한다.
- 새 음에 alternate가 없으면 기존 primary 폴백·스위치 비활성 규칙을 그대로 적용하고 `useAlternate` 값은 유지한다.
- 같은 음을 다시 선택하면 손가락 diff는 no-op일 수 있지만 사운드 attack은 새로 시작할 수 있다.

### 6.4 유효 입력의 원자적 상태 변경

한 reducer action에서 다음을 함께 갱신한다.

```text
noteIndex
octave
accidental
inputAcc
quickBand
```

그 후 한 렌더 사이클에서 다음을 갱신한다.

```text
정보 패널 제목·이명동음·음역·실음
손가락 칩
기본/대체 스위치 상태
기존 옥타브·변화표·숫자 키패드
새 3분류 UI 선택 상태와 반음 밴드 표시
SVG aria-label
aria-live
URL 해시
애니메이터 목표 pattern
```

---

## 7. 화면 UI 명세

### 7.1 배치

기존 키패드 영역에 44px 이상 높이의 compact row를 추가한다.

```text
빠른 분류  [낮은 계이름 C4] [높은 계이름 C5] [반음 C4]
옥타브     [3] [4] [5] [6]
변화표     [♭] [♮] [♯]
숫자       [1 도] [2 레] [3 미] [4 파] [5 솔] [6 라] [7 시]
```

- 새 분류는 기존 옥타브·변화표·숫자 컨트롤을 대체하지 않는다.
- 기존 숫자 버튼 한 줄을 재사용하며 같은 역할의 버튼을 24개 중복 생성하지 않는다.
- `반음 C4` 또는 `반음 C5`처럼 현재 `quickBand`를 버튼 텍스트 또는 인접 상태 텍스트로 항상 표시한다.
- 360×740 세로 화면에서 가로 스크롤이 생기지 않아야 한다.
- 세 버튼은 320px 폭에서도 최소 44px 높이를 지키고, 텍스트가 좁으면 2줄로 줄바꿈한다.
- 가로 모드에서는 기존 오른쪽 컨트롤 컬럼 안에 둔다.

### 7.2 화면 분류 버튼 동작

#### 낮은 계이름 C4

- `quickBand=4`
- `state.octave=4`, `state.accidental="natural"`
- 따라서 `deriveQuickCategory(...)`는 `"low-natural"`을 반환한다.
- 분류 버튼만 누를 때는 현재 note와 운지를 바꾸거나 소리를 내지 않는다.
- 다음 숫자 버튼/일반 숫자 키가 C4–B4 자연음을 선택한다.

#### 높은 계이름 C5

- `quickBand=5`
- `state.octave=5`, `state.accidental="natural"`
- 따라서 `deriveQuickCategory(...)`는 `"high-natural"`을 반환한다.
- 분류 버튼만 누를 때는 현재 note와 운지를 바꾸거나 소리를 내지 않는다.
- 다음 숫자 버튼/일반 숫자 키가 C5–B5 자연음을 선택한다.

#### 반음 C4/C5

- 현재 `quickBand`를 유지한다.
- `state.octave=quickBand`, `state.accidental="sharp"`
- 따라서 `deriveQuickCategory(...)`는 `"semitone"`을 반환한다.
- 숫자 1, 2, 4, 5, 6만 활성이다.
- 숫자 3과 7은 기존 §5.3 규칙대로 `aria-disabled="true"`로 표시하면서 탭 시 정확한 이명동음 토스트를 보여 준다.
- 낮은/높은 분류를 선택하면 밴드가 바뀌므로 별도의 숨은 규칙 없이 C4/C5 반음을 오갈 수 있다.

### 7.3 선택·포커스 시각

- 현재 분류 버튼은 `aria-pressed="true"`와 선택 외곽선으로 표시한다.
- 현재 음의 숫자 버튼은 **현재 selector 문맥의 MIDI가 선택 note의 MIDI와 같을 때만** 색뿐 아니라 굵은 외곽선 또는 눌림 변위로 표시한다.
- 분류·옥타브·변화표만 바꾸면 기존 운지와 note는 유지하되, 그 note가 새 selector 문맥에 속하지 않을 경우 숫자 버튼의 지속 선택 강조는 즉시 제거한다.
- 선택 강조를 `noteIndex`의 계이름 숫자만 보고 결정하지 말고 `midiFromButton(state.octave, digit, state.accidental) === currentNote.midi`와 valid 여부로 파생한다.
- 키보드 keydown 동안의 일시적 눌림과 선택된 음의 지속 강조를 서로 다른 클래스에서 관리한다.
- 반음 그룹의 3·7 비활성은 opacity만 사용하지 말고 취소선·아이콘·보조 설명 중 하나를 함께 사용한다.
- `:focus-visible` 2px 포커스 링을 유지한다.

### 7.4 도움말 문안

데스크톱 키보드 도움말 또는 설정 시트에 다음 의미가 드러나는 문안을 넣는다.

```text
Control+1–7: 낮은 계이름(C4–B4)
Shift+1–7: 높은 계이름(C5–B5)
Alt+1·2·4·5·6: 현재 반음 음역(C4 또는 C5)의 변화음
반음 음역은 마지막으로 선택한 낮은/높은 그룹을 따릅니다.
```

브라우저 제한 문안도 숨기지 않는다.

```text
일부 브라우저는 Control+숫자를 탭 이동에 사용합니다. 단축키가 전달되지 않는 환경에서는
화면의 빠른 분류 버튼을 선택한 뒤 숫자 버튼 또는 일반 숫자 키를 사용하세요.
```

---

## 8. 키보드 이벤트 계약

### 8.1 `event.code` 사용

숫자는 `event.key`가 아니라 상단 숫자열의 물리 위치를 뜻하는 `event.code`로 판별한다.

```text
Digit1 Digit2 Digit3 Digit4 Digit5 Digit6 Digit7
```

이유:

- `Shift+1`의 `event.key`는 키보드 배열에 따라 `!` 같은 기호가 되므로 숫자 비교가 실패할 수 있다.
- `KeyboardEvent.code`는 물리 키 위치를 식별하므로 한글/영문 입력 상태에 덜 의존한다.
- Alt+숫자패드는 Windows의 Alt 코드 입력과 충돌하므로 이번 기능은 `Numpad1–7`을 지원 대상으로 삼지 않는다.

### 8.2 정확히 하나의 보조키만 허용

```js
function shortcutKindFromEvent(event) {
  if (event.metaKey) return null;
  if (event.isComposing) return null;
  if (event.getModifierState?.("AltGraph")) return null;
  if (!/^Digit[1-7]$/.test(event.code)) return null;

  const count = Number(event.ctrlKey) + Number(event.shiftKey) + Number(event.altKey);
  if (count !== 1) return null;

  if (event.ctrlKey) return "low-natural";
  if (event.shiftKey) return "high-natural";
  return "semitone";
}
```

다음 조합은 무시한다.

```text
Control+Shift+숫자
Control+Alt+숫자
Shift+Alt+숫자
Meta+숫자
AltGraph+숫자
IME 조합 중 숫자
```

### 8.3 단일 전역 라우터와 우선순위

기존 일반 숫자 keydown listener 옆에 새 listener를 하나 더 붙이지 않는다. 기존 handler를 단일 전역 라우터로 통합한다.

```js
function handleGlobalKeyDown(event) {
  if (shouldIgnoreKeyboardEvent(event)) return;

  const quickKind = shortcutKindFromEvent(event);
  if (quickKind) {
    handleQuickShortcut(event, quickKind); // 여기서 처리 후 반드시 return
    return;
  }

  // 보조키가 하나라도 있는데 quick shortcut으로 인정되지 않았다면
  // 기존 일반 숫자·옥타브 handler로 절대 fall through하지 않는다.
  if (event.ctrlKey || event.shiftKey || event.altKey || event.metaKey) return;

  handleLegacyUnmodifiedKey(event); // 기존 1–7, ↑/↓, +/-
}
```

필수 우선순위:

1. editable/modal/IME 등 공통 guard
2. exact modifier 빠른 단축키
3. 보조키가 포함된 미지원 조합은 종료
4. modifier가 0개일 때만 기존 일반 키 처리

quick note의 `event.repeat`는 `handleQuickShortcut` 안에서 무시한다. 기존 화살표·설정 키의 repeat 정책은 변경하지 않는다. 이 구조로 `Control+1`을 새 handler와 기존 `1` handler가 동시에 처리하지 못하게 하며, 유효한 note 입력 한 번마다 `beginNoteInput`은 정확히 한 번만 호출한다.

### 8.4 편집 컨텍스트 보호

포커스 또는 이벤트 target이 다음 안에 있으면 전역 음악 단축키를 무시한다.

```text
input
textarea
select
[contenteditable]
열린 설정 dialog 안의 텍스트 편집 컨트롤
```

설정 시트나 modal이 키보드 포커스를 독점하는 동안은 그 컴포넌트의 정의된 키만 처리하고 음 선택 단축키를 가로채지 않는다.

### 8.5 keydown 처리 순서

1. editable, modal, IME, AltGraph, Meta 여부를 검사한다.
2. `event.code`가 `Digit1–Digit7`인지 검사한다.
3. Ctrl/Shift/Alt 중 정확히 하나인지 검사한다.
4. `event.repeat`이면 무시한다.
5. `resolveQuickRequest`로 순수 요청을 만든다.
6. 인식된 요청에만 `preventDefault()`를 호출한다.
7. invalid 반음 3·7이면 토스트만 보여 주고 종료한다.
8. valid이면 단일 reducer action으로 상태와 note를 선택한다.
9. 같은 이벤트 틱에서 기존 애니메이션과 오디오 attack을 시작한다.
10. keyup 추적용 고유 토큰을 `event.code`와 연결한다.

`preventDefault()`는 모든 숫자 키에 선제적으로 호출하지 않는다. 실제로 처리할 수 있는 단축키에서만 호출한다.

### 8.6 keyup과 고착 방지

keyup 시 modifier 상태로 요청을 다시 계산하지 않는다. 사용자가 숫자보다 Control/Shift/Alt를 먼저 뗄 수 있기 때문이다.

```js
const activeShortcutKeys = new Map(); // code → { token, midi }
```

- keydown 때 저장한 토큰을 `DigitN` keyup에서 찾는다.
- 해당 토큰이 현재 발음 voice의 토큰과 일치할 때만 release한다.
- 이전 단축키의 늦은 keyup이 새로 재생 중인 음을 끄면 안 된다.
- `window.blur`, `document.visibilitychange`, `pagehide`에서 모든 키보드 눌림 클래스와 voice를 정리한다.
- keyup은 사운드만 release하고 운지 화면은 유지한다.
- 한 키를 길게 눌러도 `event.repeat`로 애니메이션이나 attack이 반복 생성되지 않아야 한다.

### 8.7 리스너 수명

- 전역 keydown/keyup 리스너는 boot 시 한 번만 등록한다.
- 매 render마다 리스너를 추가하지 않는다.
- 개발 모드 어설션으로 같은 keydown에 handler가 한 번만 실행되는지 확인한다.

### 8.8 브라우저 예약 단축키의 현실적 처리

Chrome/Edge 등은 `Control+1–8`을 브라우저 탭 이동에 예약할 수 있다. 브라우저가 DOM으로 이벤트를 보내지 않으면 페이지 JavaScript의 capture listener나 `preventDefault()`로 복구할 수 없다.

따라서 다음을 모두 지킨다.

1. 요청한 `Control+1–7` handler는 이벤트가 전달되는 환경에서 정확히 동작하도록 구현한다.
2. 실제 대상 Chrome/Edge에서 물리 키로 수신 여부를 검사한다.
3. 브라우저가 가로챘다면 실패를 숨기거나 다른 조합으로 임의 교체하지 않는다.
4. 화면의 빠른 분류 버튼 + 일반 `1–7` 키 + 터치 숫자 버튼을 항상 동등한 폴백으로 유지한다.
5. README와 앱 도움말에 검증 결과와 알려진 제한을 적는다.
6. 자동화된 합성 `KeyboardEvent` 성공을 물리 단축키 성공으로 오인하지 않는다.

표준 참고:

- [W3C UI Events KeyboardEvent code Values](https://www.w3.org/TR/uievents-code/)
- [WAI-ARIA 1.3 aria-keyshortcuts](https://www.w3.org/TR/wai-aria-1.3/#aria-keyshortcuts)
- [Google Chrome 공식 키보드 단축키](https://support.google.com/chrome/answer/157179)

---

## 9. 접근성 계약

### 9.1 native controls

- 분류 컨트롤과 숫자 컨트롤은 native `<button>`을 사용한다.
- 분류 버튼은 `aria-pressed`를 사용하거나 올바른 tablist 패턴을 완전하게 구현한다. 두 패턴을 섞지 않는다.
- 단순한 3개 세그먼트라면 native button + `aria-pressed`를 우선한다.
- 분류 버튼만 변경했을 때 정보 패널의 음 `aria-live`를 불필요하게 다시 읽지 않는다.

### 9.2 `aria-keyshortcuts`

WAI-ARIA 표기 이름을 사용한다.

```text
Control+1
Shift+1
Alt+1
```

하나의 숫자 버튼에 여러 조합을 알릴 때는 공백으로 나열한다.

```html
<button aria-keyshortcuts="1 Control+1 Shift+1 Alt+1">...</button>
```

- 숫자 1, 2, 4, 5, 6에는 일반 숫자와 세 modifier 조합을 기술한다.
- 숫자 3, 7에는 유효하지 않은 Alt 조합을 성공 단축키로 광고하지 않는다.
- `aria-keyshortcuts`만 제공하고 화면 설명을 생략하지 않는다.

### 9.3 동적 설명

- 반음 분류의 접근 가능한 이름에 현재 밴드를 포함한다: `반음, 현재 C4 음역`.
- invalid 입력 토스트 컨테이너는 기존처럼 `role="status"`를 사용한다.
- 음 선택 후 `aria-live`는 음 이름, 영문 기보음, 음역, 활성 손가락을 한 번만 읽는다.
- 선택 상태는 색 외에 `aria-pressed`, 외곽선, 위치 변화로도 구분한다.
- reduced-motion에서는 단축키 경로도 즉시 최종 상태로 이동한다.

---

## 10. 애니메이션·오디오 통합 계약

### 10.1 애니메이션

새 단축키는 반드시 기존 `animator.transitionTo(pattern)`을 호출한다.

- 시작 상태 `noteIndex=null`이면 이전 pattern은 `[]`다.
- 현재 음의 목표 pattern은 기존 규칙대로 primary 또는 alternate다.
- 키보드 입력이라고 스태거, SLIDE, linked 키, HOLD 신규 키 채움 규칙을 생략하지 않는다.
- 연타 시 기존 목표 pattern과 현재 손끝 좌표를 사용하는 인터럽트 정책을 그대로 적용한다.
- 같은 음 재입력은 SVG 노드 재생성이나 불필요한 손가락 흔들림 없이 no-op diff가 되어야 한다.
- 전환 전후 장면의 SVG 노드 수가 같아야 한다.

### 10.2 오디오

- keydown: 선택·애니메이션 시작과 같은 틱에 attack
- keyup: 200ms release tail
- 새 음 keydown: 이전 음은 기존 60ms fade-out 후 새 음 attack
- 재생 높이: 기존 설정에 따라 기보음 또는 악기별 실음
- 첫 사용자 keydown도 사용자 제스처이므로 필요하면 `AudioContext.resume()`을 호출한다.
- 소리 설정이 꺼져 있으면 AudioContext를 불필요하게 만들지 않는다.
- invalid Alt+3/7은 attack하지 않는다.
- blur/visibilitychange/pagehide에서 고착 음을 정리한다.

### 10.3 필수 전환 시나리오

아래 표의 구체적인 키 ID·direct/linked 기대값은 별도 표기가 없으면 `useAlternate=false`인 **기본 운지 모드**에서 검사한다. 단축키 handler가 테스트를 맞추기 위해 `useAlternate`를 false로 바꾸어서는 안 된다. 아래 기본 운지 검사를 마친 뒤 `useAlternate=true`의 별도 시나리오도 실행한다.

| 시나리오 | 기대 결과 |
|---|---|
| `Control+1 → Control+2` | C4→D4. `[3,4,8,13] → [3,4,8]`, L3 하나만 RELEASE |
| `Control+5` | G4. 모든 구멍 열림, RT 지지는 유지 |
| `Control+5 → Alt+5` | G4→G♯4. L1이 키 2를 누름; L4가 아님 |
| `Control+1 → Alt+1` | C4→C♯4. L4 목표 키 11, 키 12는 linked 점선 |
| `Shift+5 → Alt+5` | G5→G♯5. 키 11 direct, 키 12 linked |
| `Alt+6(C4 밴드) → Control+7`, 사전조건 `useAlternate=false` | A♯4/B♭4→B4 브레이크 통과. 양손 대규모 재배치와 키 24 linked 확인 |
| `Control+7 → Shift+1` | B4→C5 옥타브 그룹 경계에서 state·URL·운지 동기화 |
| `Control+1 → Control+2 → Shift+1 → Alt+1` 빠른 연타 | 현재 보간 위치에서 이어지고 손가락 점프 없음; 마지막 음 C♯5 |
| 같은 유효 조합 재입력 | 운지 diff no-op, 사운드는 정상 재발음 |
| `Alt+3`, `Alt+7` | 운지·URL·사운드 불변, 정확한 이명동음 토스트만 표시 |

대체 운지 유지 전용 시나리오:

```text
사전조건: useAlternate=true
Alt+6(C4 밴드) → Control+7
기대 pattern: A♯4 alternate [0,6] → B4 alternate [1,3,4,8,13,17,18,20,23]
기대: 키 24 linked를 요구하지 않으며, useAlternate는 true로 유지되고 기존 diff 엔진이 두 대체 배열을 비교한다.
```

---

## 11. 단계별 구현 절차와 단계별 통과 조건

### Phase 0 — 파일 인벤토리와 기반 분기

#### 작업

1. `rg --files` 등으로 대상 HTML, 테스트, README, `01 Source`를 확인한다.
2. 기준 개발서 전체를 읽는다.
3. 원본 HTML에서 5개 상수를 프로그램으로 추출한다.
4. 원본 자체의 `DATA.length=42`, alternate=19, MIDI 52–93, COMPONENTS=25를 검증한다.
5. 대상 앱 존재 여부로 다음 분기를 명시한다.
   - **기존 앱 분기**: 현재 입력→state→render→animator→audio 흐름과 변경 전 테스트 결과를 기록한다.
   - **신규 앱 분기**: `기준선 없음`을 정상 상태로 기록하고, 기준 개발서 Phase 0–5의 기반 앱·테스트·README 생성을 Phase 1 선행 작업으로 예약한다.
6. 파일이 없다는 사실 자체를 테스트 실패나 blocker로 보고하지 않는다.
7. 작업 전 파일 목록, 선택한 분기, 실제로 존재해 실행한 테스트만 기록한다.

#### 통과 조건

```text
DATA.length === 42
alternate 보유 음 === 19
MIDI 52–93 연속
COMPONENTS.length === 25
원본 HTML 내부 데이터 어설션 통과
```

원본 자체 어설션이 실패하면 다음 단계로 넘어가지 않는다. 대상 앱이 없는 신규 분기에서는 아직 앱과 비교할 수 없으므로 이 시점의 `원본 데이터 diff 0`을 요구하거나 성공으로 보고하지 않는다.

### Phase 1 — 회귀 기준선 생성 또는 고정

#### 작업

1. **기존 앱 분기**에서는 존재하는 `tests/validate-data.mjs`와 `tests/keypad-coverage.mjs`를 실행한다. 누락된 테스트는 기준 개발서 명세대로 먼저 생성해 실행하며, 없는 테스트를 성공으로 간주하지 않는다.
2. **신규 앱 분기**에서는 다음 순서로 최초 기준선을 만든다.
   1. 원본 5개 상수를 프로그램으로 추출해 기준 개발서대로 앱 DATA 구획을 만든다.
   2. `clarinet_solfege_trainer.html`의 기반 상태·장면·animator·UI·audio를 기준 개발서 Phase 0–5대로 구현한다.
   3. `tests/validate-data.mjs`, `tests/keypad-coverage.mjs`, `README.md`를 만든다.
   4. 생성된 앱과 원본을 처음 비교해 diff 0을 확인한다.
   5. 생성된 기존 키패드로 42음 도달성을 확인한다.
   6. 이 시점을 `최초 기준선`으로 기록한 뒤 빠른 분류 Phase 2로 간다.
3. 개발서 §14.2 예시의 잘못된 `[1,...,7]` 표현은 실제 코드에 복사하지 말고 `[1,2,3,4,5,6,7]`을 사용한다.
4. 대상 HTML을 열어 콘솔 오류, 네트워크 요청, 시작 화면을 확인한다.
5. 기존 앱 분기의 변경 전 실패는 새 기능 실패와 구분해 기록한다.

#### 통과 조건

- 원본 데이터 diff 0
- 기존 42음 도달 가능
- 테스트 스크립트 자체에 문법/런타임 오류 없음
- 기존 앱은 변경 전 기준선, 신규 앱은 생성 후 최초 기준선 결과가 기록됨
- 이 Phase 종료 시에는 두 분기 모두 대상 앱·두 회귀 테스트·README가 실제로 존재함

### Phase 2 — 단축키 매핑 순수 함수와 테스트를 먼저 작성

#### 작업

1. `resolveQuickRequest`를 DOM과 분리해 구현한다.
2. low 7건, high 7건, semitone 각 밴드 5건의 기대값 테스트를 작성한다.
3. Alt+3/7 invalid 테스트를 작성한다.
4. 24개 합집합과 MIDI 60–83 연속성을 검증한다.
5. 모든 유효 MIDI가 `NOTE_BY_MIDI`에 있는지 검증한다.
6. §12.0의 marker+Node `vm` 연결부로 **실제 HTML의 제품 함수**를 테스트한다.

#### 통과 조건

- 표 §4.2–4.4와 MIDI/id가 1건도 다르지 않음
- valid 목표 24개 고유
- 매핑 코드에 운지 키 ID나 `primary/alternate` 복사본 없음
- invalid 요청이 상태 변경 없이 토스트에 필요한 정보를 제공함

### Phase 3 — 공용 선택 파이프라인 리팩터링

#### 작업

1. 기존 포인터 숫자 선택 로직을 공용 `selectNoteRequest` 또는 동등한 reducer action으로 모은다.
2. `quickBand` 상태와 `deriveQuickCategory` 파생 함수를 추가한다.
3. state의 5개 필드(`noteIndex`, `octave`, `accidental`, `inputAcc`, `quickBand`)를 원자적으로 갱신한다.
4. render, URL, animator 호출을 한 번만 수행한다.
5. alternate 유지와 primary 폴백을 확인한다.
6. 같은 음 재선택 동작을 정의한다.
7. `selectNoteRequest`와 `beginNoteInput`의 오디오 소유권을 §6.3대로 분리한다.

#### 통과 조건

- 기존 터치 숫자 입력 결과가 변경 전과 동일
- `Control+1` 요청 후 `note=C4`, octave=4, natural
- `Shift+7` 요청 후 `note=B5`, octave=5, natural
- `Shift+1` 뒤 Alt+1 요청은 Cs5
- `Control+1` 뒤 Alt+1 요청은 Cs4
- 유효 입력 한 번에 dispatch·render·attack 각 1회, animator 1회 이하

### Phase 4 — 3분류 화면 UI

#### 작업

1. 빠른 분류 row를 추가한다.
2. 세 native button을 reducer에 연결한다.
3. 분류 버튼은 note를 즉시 바꾸지 않고 기존 키패드 문맥만 바꾼다.
4. 반음 버튼에 현재 C4/C5 밴드를 표시한다.
5. 숫자 라벨과 disabled 상태를 기존 렌더러로 갱신한다.
6. 선택·포커스·pressed 시각을 추가한다.
7. 세로·가로 레이아웃을 확인한다.

#### 통과 조건

- 터치만으로 세 분류의 24음을 모두 선택 가능
- C4/C5 반음 문맥이 눈에 보임
- 320px 폭에서 가로 스크롤 없음
- 모든 새 터치 타깃 최소 44×44px
- 기존 옥타브 3·6과 플랫 선택이 계속 가능

### Phase 5 — 키보드 adapter

#### 작업

1. `shortcutKindFromEvent`를 구현한다.
2. `Digit1–Digit7`만 사용한다.
3. exact modifier, editable, modal, IME, AltGraph, repeat guard를 구현한다.
4. recognized 요청에만 `preventDefault()`를 적용한다.
5. keydown token과 keyup release를 구현한다.
6. blur/visibility/pagehide cleanup을 구현한다.
7. listener 중복 등록 방지 어설션을 넣는다.
8. 기존 일반 키 handler를 단일 `handleGlobalKeyDown`으로 합치고 modifier 우선순위를 적용한다.

#### 통과 조건

- Shift+1이 `!`로 오인되지 않음
- Ctrl+Shift+1, Ctrl+Alt+1, Meta+1, AltGraph+1 무시
- input/textarea/select/contenteditable 편집을 방해하지 않음
- repeat로 중복 attack/애니메이션 없음
- modifier를 먼저 떼도 keyup release 정상
- 이전 keyup이 새 voice를 끄지 않음
- 포커스 이탈 후 음 고착 없음
- `Control+1`이 기존 일반 `1` handler로 fall through하지 않음

### Phase 6 — 애니메이션과 오디오 연결

#### 작업

1. 모든 유효 shortcut을 기존 pattern 결정 함수에 연결한다.
2. keydown에서 animation과 attack을 시작한다.
3. keyup에서 release하고 운지는 유지한다.
4. §10.3의 필수 전환을 실행한다.
5. reduced-motion과 즉시 모드를 확인한다.
6. §10.3의 대체 운지 유지 전용 시나리오를 실행하고 기본 운지 기대값과 섞지 않는다.

#### 통과 조건

- §10.3 전환 기대값 통과
- 손가락 점프·SVG DOM 재생성 없음
- direct/linked 스타일 유지
- keydown과 발음 사이 인위적 애니메이션 완료 대기 없음
- 무음 설정과 기보음/실음 설정 유지

### Phase 7 — 접근성과 도움말

#### 작업

1. `aria-pressed`, accessible name, `aria-keyshortcuts`를 적용한다.
2. invalid 토스트와 정보 패널 live region을 검사한다.
3. 키보드 도움말과 브라우저 제한 문안을 추가한다.
4. 포커스 링, 대비, 흑백 판독, reduced-motion을 확인한다.

#### 통과 조건

- 분류와 밴드를 스크린리더가 이해 가능
- aria-live 중복 낭독 없음
- 숫자 3·7이 Alt 성공 단축키로 광고되지 않음
- 색 없이도 선택/disabled 판별 가능
- 도움말에 Ctrl 브라우저 충돌과 화면 폴백이 명시됨

### Phase 8 — 자동 테스트와 정적 검증

#### 작업

1. `tests/keyboard-shortcuts.mjs`를 완성한다.
2. 원본 데이터 회귀와 기존 도달성 테스트를 다시 실행한다.
3. HTML 구문, 중복 id, 콘솔 오류를 검사한다.
4. 네트워크 요청이 0인지 검사한다.
5. 가능하면 실제 브라우저 자동화로 포인터·키보드·URL 복원을 확인한다.

#### 통과 조건

- 모든 자동 테스트 통과
- DATA diff 0
- 기존 42음 도달성 유지
- 콘솔 오류 0
- 외부 네트워크 요청 0
- `file://` 실행 정상

### Phase 9 — 실제 브라우저와 기기 검증

#### 작업

1. 데스크톱 Chrome과 Edge에서 상단 숫자열을 물리 키로 테스트한다.
2. Ctrl 조합의 이벤트 수신 여부를 각각 기록한다.
3. Shift, Alt, modifier 해제 순서, 빠른 연타를 테스트한다.
4. iPhone Safari와 Android Chrome에서 화면 분류 버튼으로 24음을 선택한다.
5. 세로 360×740, 가로 max-height 500px를 확인한다.
6. 첫 입력 오디오 unlock과 창 전환 후 cleanup을 확인한다.

#### 통과 조건

- 실제 결과가 브라우저별로 기록됨
- Ctrl가 브라우저에 가로채져도 화면+일반 숫자 폴백 정상
- 모바일에서 modifier 없이 전체 기능 사용 가능
- 레이아웃 오버플로 없음
- 첫 탭/키 입력 오디오와 release 정상

### Phase 10 — 문서화와 완료 보고

#### 작업

1. README에 매핑표, 반음 밴드 규칙, 실행법, 테스트법을 추가한다.
2. 브라우저 예약키의 알려진 제한을 기록한다.
3. 변경 파일과 검증 결과를 아래 형식으로 보고한다.
4. 미검증 항목을 `통과`로 쓰지 않는다.

---

## 12. `tests/keyboard-shortcuts.mjs` 필수 테스트 명세

### 12.0 제품 코드 테스트 연결부(test seam)

테스트 파일에 `resolveQuickRequest`, `deriveQuickCategory`, `shortcutKindFromEvent`, MIDI 계산 로직을 복사하지 않는다. 복사본을 검증하면 제품 코드가 틀려도 테스트가 통과하는 자기복제 테스트가 된다.

대상 HTML의 THEORY/STATE 순수 함수 구간을 다음 marker로 한 번만 감싼다.

```js
/* TESTABLE_SHORTCUT_CORE_START */
const PC = { /* 기준 개발서의 실제 값 */ };
const ACC = { /* 기준 개발서의 실제 값 */ };
function midiFromButton(/* ... */) { /* 실제 제품 구현 */ }
const SEMITONE_DEGREES = new Set([1, 2, 4, 5, 6]);
function deriveQuickCategory(/* ... */) { /* 실제 제품 구현 */ }
function resolveQuickRequest(/* ... */) { /* 실제 제품 구현 */ }
function shortcutKindFromEvent(/* ... */) { /* 실제 제품 구현 */ }
/* TESTABLE_SHORTCUT_CORE_END */
```

규칙:

- marker 사이에는 DOM, AudioContext, SVG, boot side effect를 넣지 않는다.
- `PC`, `ACC`, `midiFromButton`이 이미 THEORY에 있으면 옮겨 감쌀 뿐 두 번째 복사본을 만들지 않는다.
- marker 문자열은 HTML에서 정확히 한 쌍만 존재해야 한다.
- 브라우저에서는 이 블록이 평소처럼 제품 코드로 실행된다.
- 프로덕션 `window`에 테스트 전용 export를 상시 노출하지 않는다.

`tests/keyboard-shortcuts.mjs`는 Node 내장 모듈만 사용해 다음 순서로 실제 코드를 불러온다.

```js
import assert from "node:assert/strict";
import fs from "node:fs";
import vm from "node:vm";

const html = fs.readFileSync(new URL("../clarinet_solfege_trainer.html", import.meta.url), "utf8");
const startMark = "/* TESTABLE_SHORTCUT_CORE_START */";
const endMark = "/* TESTABLE_SHORTCUT_CORE_END */";
const start = html.indexOf(startMark);
const end = html.indexOf(endMark);

assert(start >= 0 && end > start);
assert.equal(html.indexOf(startMark, start + startMark.length), -1);
assert.equal(html.indexOf(endMark, end + endMark.length), -1);

const productCode = html.slice(start + startMark.length, end);
const context = vm.createContext({ console });
vm.runInContext(`${productCode}\n;globalThis.__shortcutTest = {
  midiFromButton,
  deriveQuickCategory,
  resolveQuickRequest,
  shortcutKindFromEvent
};`, context);

const core = context.__shortcutTest;
```

위 코드는 방법을 고정하는 예시다. 오류 문맥을 보완하되 외부 패키지는 추가하지 않는다. 기대 MIDI/id 배열만 테스트 fixture로 둘 수 있으며, 제품 함수 구현을 테스트 파일에 재작성해서는 안 된다.

DOM target 판별, 실제 event listener, render/animator/audio 호출 횟수는 다음 두 층으로 보완한다.

1. 브라우저 통합 테스트에서 실제 HTML의 handler를 실행한다.
2. `beginNoteInput` 의존성을 spy 가능한 함수로 주입하거나 dev test hook에서 카운터를 읽어 `dispatch=1`, `animator≤1`, `attack=1`을 검증한다. test hook은 테스트 모드에서만 활성화한다.

### 12.1 자연음 매핑

```js
assert.deepStrictEqual(lowMidi,  [60,62,64,65,67,69,71]);
assert.deepStrictEqual(highMidi, [72,74,76,77,79,81,83]);
assert.deepStrictEqual(lowIds,   ["C4","D4","E4","F4","G4","A4","B4"]);
assert.deepStrictEqual(highIds,  ["C5","D5","E5","F5","G5","A5","B5"]);
```

### 12.2 변화음 매핑

```js
assert.deepStrictEqual(lowChromaticMidi,  [61,63,66,68,70]);
assert.deepStrictEqual(highChromaticMidi, [73,75,78,80,82]);
assert.deepStrictEqual(validSemitoneDegrees, [1,2,4,5,6]);
```

### 12.3 합집합

```js
const all = new Set([
  ...lowMidi,
  ...highMidi,
  ...lowChromaticMidi,
  ...highChromaticMidi,
]);
assert.equal(all.size, 24);
for (let midi = 60; midi <= 83; midi++) assert(all.has(midi));
```

### 12.4 밴드 상태

다음 시퀀스를 순수 reducer 또는 테스트 가능한 상태 함수로 검증한다.

```text
초기 quickBand=4
Shift+1 → C5, quickBand=5
Alt+2   → D♯5, quickBand=5
Control+1 → C4, quickBand=4
Alt+6   → A♯4, quickBand=4
전체 UI octave=6 → quickBand는 4 유지
전체 UI octave=5 → quickBand=5
```

### 12.5 invalid와 상태 불변

각 밴드에서 Alt+3, Alt+7을 검사한다.

```text
noteIndex 불변
현재 pattern 불변
URL hash 불변
오디오 attack 0회
animator 호출 0회
토스트 1개
토스트 타이머 중복 생성 없음
```

### 12.6 이벤트 파서

mock KeyboardEvent 성격의 객체로 다음을 검증한다.

```text
Control+Digit1 → low-natural
Shift+Digit1   → high-natural
Alt+Digit1     → semitone
Shift+Digit1의 key가 "!"여도 code로 성공
Control+Shift+Digit1 → null
Control+Alt+Digit1 → null
Meta+Digit1 → null
AltGraph+Digit1 → null
isComposing=true → null
Numpad1 → null
Digit8 → null
repeat=true → handler가 선택하지 않음
editable target → handler가 선택하지 않음
Control+Digit1 처리 시 legacy unmodified handler 호출 0회
유효 shortcut 1회 시 dispatch=1, render=1, attack=1, animator≤1
invalid Alt+3/7 시 dispatch=0, animator=0, attack=0, toast=1
```

### 12.7 기존 기능 회귀

- `tests/validate-data.mjs`: 원본과 앱 상수 deepStrictEqual
- `tests/keypad-coverage.mjs`: E3–A6 42음 도달
- 대체 운지 19개 유지
- linked로 판정되는 키는 12·24뿐
- URL의 샤프 id는 `Cs4`, `Fs5` 형식 유지
- B♭/A 실음 계산 유지

---

## 13. 수동 검증 체크리스트

### 13.1 매핑

- [ ] Control+1–7 요청이 C4, D4, E4, F4, G4, A4, B4와 일치
- [ ] Shift+1–7 요청이 C5, D5, E5, F5, G5, A5, B5와 일치
- [ ] C4 밴드의 Alt+1·2·4·5·6이 C♯4, D♯4, F♯4, G♯4, A♯4와 일치
- [ ] C5 밴드의 Alt+1·2·4·5·6이 C♯5, D♯5, F♯5, G♯5, A♯5와 일치
- [ ] Alt+3/7은 상태 불변 + 정확한 이명동음 토스트
- [ ] 샤프 제목과 플랫 이명동음 부제가 함께 표시
- [ ] 24개 음의 운지가 기존 DATA와 일치

### 13.2 상태·UI

- [ ] 낮은/높은 선택이 quickBand 4/5를 갱신
- [ ] 반음 버튼에 현재 C4/C5 밴드가 보임
- [ ] 전체 옥타브 3/6 선택 후에도 마지막 quickBand가 예측 가능하게 유지
- [ ] 기존 옥타브·변화표·숫자 키패드 정상
- [ ] E3–B3, C6–A6 터치 접근 유지
- [ ] 대체 운지 상태 유지
- [ ] URL 저장·새로고침 복원 정상
- [ ] 시작 화면 `noteIndex=null` 규칙 유지

### 13.3 애니메이션

- [ ] §10.3 전 시나리오 통과
- [ ] 빠른 연타에서 손가락 점프 없음
- [ ] direct/linked 표시 정확
- [ ] 키 2는 L1, 키 19는 R2
- [ ] 키 24 연동 예외 유지
- [ ] 같은 음 재선택에서 불필요한 손가락 모션 없음
- [ ] 전환 전후 SVG 노드 수 불변
- [ ] reduced-motion 즉시 상태 정상

### 13.4 키보드·오디오

- [ ] `event.code` 기반이라 Shift 기호 문제 없음
- [ ] exact modifier guard 정상
- [ ] IME·AltGraph·editable 컨텍스트 보호
- [ ] key repeat 중복 없음
- [ ] modifier 역순 해제 정상
- [ ] 이전 keyup이 새 음을 끄지 않음
- [ ] blur/visibility/pagehide 후 고착 음 없음
- [ ] keydown attack, keyup release, 운지 유지
- [ ] iOS 첫 사용자 입력 오디오 unlock 정상

### 13.5 접근성·레이아웃

- [ ] 새 버튼 native button
- [ ] `aria-pressed`, `aria-keyshortcuts`, 도움말 정확
- [ ] aria-live 중복 낭독 없음
- [ ] invalid 이유를 색 외에도 알 수 있음
- [ ] focus-visible 링 유지
- [ ] 320·360px 세로 폭에서 가로 스크롤 없음
- [ ] 가로 모드 max-height 500px 정상
- [ ] 터치 타깃 최소 44px

### 13.6 회귀·실행

- [ ] DATA 42개, alternate 19개
- [ ] 원본 데이터 diff 0
- [ ] 42음 기존 키패드 도달성 유지
- [ ] 콘솔 오류 0
- [ ] 네트워크 요청 0
- [ ] `file://` 더블클릭 실행 정상
- [ ] Chrome/Edge Ctrl 실기 결과가 README와 보고서에 기록됨

---

## 14. 완료 기준(Definition of Done)

다음 조건을 모두 만족해야 완료다.

1. 낮은 자연음 7개, 높은 자연음 7개, C4/C5 변화음 10개가 화면 UI로 모두 선택된다.
2. 이벤트가 페이지에 전달되는 환경에서 요청한 Control/Shift/Alt 조합이 §4 표와 정확히 일치한다.
3. Ctrl 브라우저 예약키의 실기 결과와 화면 폴백이 문서화되어 있다.
4. 모든 유효 입력이 기존 `NOTE_BY_MIDI → reducer → render → animator → audio` 경로를 사용한다.
5. 새 운지 배열이나 음별 애니메이션 복제물이 없다.
6. §10.3의 전환 시나리오가 통과한다.
7. keyboard shortcut 자동 테스트가 모두 통과한다.
8. 원본 DATA diff가 0이다.
9. 기존 42음 도달성, 대체 운지 19개, URL, 설정, 접근성이 회귀하지 않는다.
10. 콘솔 오류 0, 네트워크 요청 0, `file://` 실행 정상이다.
11. 360×740 세로와 가로 레이아웃에서 오버플로가 없다.
12. reduced-motion과 오디오 cleanup이 정상이다.
13. 미검증 항목과 브라우저가 가로챈 조합을 성공으로 보고하지 않는다.

---

## 15. 완료 보고 형식

작업 종료 시 다음 형식을 사용한다.

```markdown
## 변경·생성 파일
- 앱: ...
- 테스트: ...
- 문서: ...

## 24개 음 매핑 검증
- 낮은 자연음 7개: 통과/실패
- 높은 자연음 7개: 통과/실패
- C4 변화음 5개: 통과/실패
- C5 변화음 5개: 통과/실패
- Alt+3/7 상태 불변: 통과/실패

## 애니메이션·오디오 검증
- 필수 전환 시나리오: ...
- 연타 인터럽트: ...
- keydown/keyup 및 cleanup: ...

## 데이터 회귀
- DATA: 42
- alternate: 19
- MIDI: 52–93 연속
- 원본 diff: 0/실패
- 기존 키패드 도달: 42/42

## 자동 테스트
- 실행 명령: ...
- 통과/실패 건수: ...

## 실제 브라우저 결과
- Chrome Control+1–7: 이벤트 수신/브라우저 가로챔/미검증
- Edge Control+1–7: 이벤트 수신/브라우저 가로챔/미검증
- Shift+1–7: ...
- Alt+1·2·4·5·6: ...
- 모바일 화면 폴백: ...

## 알려진 제한
- ...

## 남은 작업
- 없음 / 구체적 항목
```

`원본 diff 0`, `42/42`, 실제 브라우저 결과는 실행 증거 없이 추정해 쓰지 않는다.

---

## 16. 최종 실행 지시문

위 요구사항을 순서대로 구현하라. 먼저 기준 개발서와 원본 HTML을 읽고 데이터 diff 0과 기존 42음 도달성을 기준선으로 고정하라. 대상 앱이 없으면 기준 개발서대로 기반 앱을 구현한 뒤 같은 작업 흐름에서 3분류 기능을 추가하고, 앱이 있으면 기존 구조를 보존하며 증분 수정하라.

낮은 계이름은 C4–B4, 높은 계이름은 C5–B5로 고정한다. 반음은 C♯·D♯·F♯·G♯·A♯이며, `Alt+숫자`는 화면에 표시되는 `quickBand` 4 또는 5를 사용한다. `Alt+3/7`은 자동 이명동음 이동 없이 토스트만 보여 준다. 모든 선택은 기존 DATA와 공용 reducer·render·animator·audio 경로를 사용하고, 새 운지 배열이나 별도 애니메이터를 만들지 마라.

순수 매핑 테스트를 먼저 통과시킨 다음 UI, 키보드 adapter, 애니메이션·오디오, 접근성을 연결하라. 마지막에는 원본 데이터 diff, 42음 회귀, 24개 매핑, 필수 전환, 실제 Chrome/Edge의 Control 예약키 결과, 모바일 화면 폴백을 검증하고 §15 형식으로 보고하라. 계획만 제시하거나 브라우저가 가로챈 단축키를 성공했다고 간주하지 마라.
